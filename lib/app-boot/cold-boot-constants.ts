/** Shared cold-boot constants — safe for server layout + client modules. */

export const COLD_BOOT_SESSION_KEY = "dibay:cold-boot:session-marked";

export const DIBAY_COLD_BOOT_INTRO_DOM_ID = "dibay-cold-boot-intro";
