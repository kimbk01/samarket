/**
 * Reuses `subscribeTabLeader` — Web/Admin audible owner is one visible/focused leader tab.
 * Scope must start at app lifetime (`NotificationSoundLeaderBootstrap`), not route Prime.
 */
export const SAMARKET_NOTIFICATION_SOUND_LEADER_SCOPE = "notification-sound";
