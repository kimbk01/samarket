/** Event media storage — reserved Phase 2 bucket; shared Admin image validation. */
export const PLATFORM_EVENT_MEDIA_BUCKET = "platform-event-media";
/** Match storage.buckets.file_size_limit (5MB). */
export const PLATFORM_EVENT_MEDIA_MAX_BYTES = 5 * 1024 * 1024;
