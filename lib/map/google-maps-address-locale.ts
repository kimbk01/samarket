/**
 * 주소·Places·Geocoder 응답 언어 — 기기/앱 UI 언어와 무관하게 Google 에서 **영어**만 사용.
 * @see https://developers.google.com/maps/documentation/javascript/localization
 */
export const GOOGLE_MAPS_ADDRESS_LANGUAGE = "en" as const;

/** 필리핀 바이어스(ccTLD). `language` 과 별개. */
export const GOOGLE_MAPS_ADDRESS_REGION = "PH" as const;
