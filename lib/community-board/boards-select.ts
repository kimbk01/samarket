/** `boards` — `mapBoardRow` / `Board` 타입에 필요한 컬럼 */
export const COMMUNITY_BOARDS_ROW_SELECT =
  "id, service_id, name, slug, description, skin_type, form_type, category_mode, policy, is_active, sort_order";
