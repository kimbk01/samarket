/** Community feed list thumb — md box 88px (`COMMUNITY_FEED_LIST_THUMB_BOX_CLASS`). */
export const COMMUNITY_FEED_THUMB_DISPLAY_PX = 88;

/** Trade feed list thumb — md box 120px (`TRADE_FEED_THUMB_BOX_CLASS`). */
export const TRADE_FEED_THUMB_DISPLAY_PX = 120;

/** LCP: first viewport row only — eager/high priority. */
export const FEED_LCP_PRIORITY_COUNT = 3;
