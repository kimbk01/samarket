/**
 * @deprecated `useMessengerUIStore` + `useCallStore` 로 분리되었습니다. 새 코드는 `@/lib/community-messenger/stores` 를 사용하세요.
 */
export { useMessengerUIStore as useMessengerRoomUiStore } from "./useMessengerUIStore";
