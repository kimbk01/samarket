import {
  APP_MAIN_GUTTER_NEG_X_CLASS,
} from "@/lib/ui/app-content-layout";

/**
 * TRADE 본문 래퍼
 * 부모 컬럼 거터를 상쇄해, 내부 목록이 홈(`/home`)과 동일 인셋 토큰을 직접 쓰게 한다.
 */
export const TRADE_CONTENT_SHELL_CLASS = APP_MAIN_GUTTER_NEG_X_CLASS;
