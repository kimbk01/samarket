/** 전 채팅 표면 stick-to-bottom 판정(px) — CM tuning 과 동일 SSOT */
export const CHAT_THREAD_STICK_THRESHOLD_PX = 96;

/** entry terminal bottom — layout commit 후 1회 scroll 재시도 */
export const CHAT_THREAD_ENTRY_SCROLL_MAX_ATTEMPTS = 12;
