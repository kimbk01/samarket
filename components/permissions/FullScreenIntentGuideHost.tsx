"use client";

/**
 * FSI in-app guide — deferred (lock-screen receive failure follow-up only).
 * Login/resume auto-display disabled for OS-only permission UX.
 */
export function FullScreenIntentGuideHost() {
  return null;
}
