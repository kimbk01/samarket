# DIBAY Notification Preference Fail-Closed HARD LOCK

**ACTIVE.** Full contract: `docs/dibay-notification-preference-fail-closed-hard-lock.md`  
Evidence: `.tmp/notification-cd1-fail-closed/`

## LOCKED

Optional push preference gate is **fail-closed** on throw / non-missing preference DB error.

- `dispatch-push-for-user.ts`: `.catch(() => false)` only
- `campaign-eligibility.ts`: `.catch(() => false)` only
- Preference reader: missing relation → null (**NO_ROW**); other errors → throw (**READ_FAILED**)
- `shouldSendWebPushForUser`: READ_FAILED + optional → false; READ_FAILED + **mandatory** → true
- `skip_settings_gate` for call/system paths: **PRESERVED**
- Inbox `notification_events` create is **not** blocked by this gate

## DO NOT

- Restore `.catch(() => true)` on preference gates
- Collapse non-missing preference DB errors to defaults-ON
- Fail-close mandatory push on preference READ_FAILED
- Reopen CD-1 for speculative cleanup
- Touch Call functional HARD LOCK or Android Page Navigation HARD LOCK under this rule

## Change gate

Preference / web-push settings gate / campaign push eligibility → **regression-only**. Reopen only on Owner PRODUCT FAIL + first divergence.
