---
description: HARD LOCK — DIBAY Feed Banner product (surface/cadence/selector/creatives/financial/native)
alwaysApply: true
---

# DIBAY Feed Banner Product HARD LOCK

**HARD LOCK (2026-08-10).** Full doc: `docs/dibay-feed-banner-product-hard-lock.md`.  
**Surface/cadence/slot-items reopen:** community SSOT connect (ALL→COMMUNITY_HOME, gaps 4–6, max 3 campaigns/slot).

## Baseline

- **PRODUCT CONTRACT CHANGE RUNTIME: PASS** — `FIRST BREAK: NONE`
- Commit: `b0359b90b` · Prod: `dpl_EG56Virz2MPdgJXtXLpg4E5qvw6D` · Alias: `https://samarket.vercel.app`
- Evidence: `.qa-logs/feed-banner-runtime-evidence-20260810/REPORT.json` · `R6-FINAL.json`
- Production smoke: `.qa-logs/feed-banner-prod-smoke-20260810/`
- **NOT claimed:** Legacy cleanup · Analytics · Native Cap

## KEEP (without reopen — doc §1)

| Concern | Decision |
|---|---|
| Community surface | **ALL** (latest\|popular)→`COMMUNITY_HOME`; TOPIC→`COMMUNITY_TOPIC:<slug>`; LOCAL→no banner |
| ALL pool | HOME + all active TOPIC → rotation |
| TOPIC pool | Matching TOPIC only |
| Surface SSOT | `resolveCommunityFeedSurface` |
| Cadence | Gaps **4–6** deterministic (`FeedAdSlotPolicy`) |
| Session | `feedSessionId` stable per surfaceKey (not remount-minted) |
| Selector | Stable hash + anti-repeat; up to **3 campaigns/slot** (`selectCampaignsForPlacement`) |
| Member limit | One current campaign; 409 + HOLD 0 on second; terminal → new OK |
| Creatives | 1 campaign = **1–3** creatives (metadata); slot items ≠ creative slides |
| Empty pool | `null` / blank height 0 |
| Geometry | Card-rhythm fixed height + cover (not hero 3:1) |
| Financial | HOLD / CAPTURE / RELEASE / RENEW SSOT |
| Sanitation | Reachable HTTPS creatives only |
| Native | ZERO CHANGE |

## DO NOT (without reopen — doc §3)

- Restore gaps **6–10** or day-bucket-only as product defaults
- Bypass 409 / invent second current HOLD
- Revert creative to unbounded aspect-3:1 + object-contain hero strip
- Dual CODE+DB runtime prices
- Mix Legacy cleanup / Analytics into banner product work
- Touch Native Cap / Auth / Push / Badge for Feed Banner
- Merge Post promotion (A) or Admin Direct (C) into member Feed Banner (B)
- Raw ledger/balance patch bypassing `adjustUserPoints` / hold writers
- Call residual deferred tracks “PASS” without new evidence
- Cross-topic bleed on TOPIC feeds / invent multi-topic schema
- Treat raw `feed_ad_requests.status=active` as feed-running “광고중” without campaign status/window
- End a campaign without syncing the linked request to ended

## Authority (summary)

| Concern | Owner |
|---|---|
| Surface | `resolveCommunityFeedSurface` |
| Cadence | `lib/ads/feed-ad-slot-policy.ts` |
| Session | `lib/ads/feed-ad-session.ts` |
| Select + anti-repeat | `selectCampaignsForPlacement` |
| Create + HOLD | `POST /api/me/feed-ad-requests` |
| Approve CAPTURE | `approve-feed-ad-request.ts` |
| Reject/cancel RELEASE | approve reject · `cancel-feed-ad-request.ts` |
| Renew | `renew-feed-ad-campaign.ts` |
| Price/period | `feed_ad_products` DB + readers |
| Feed active | `GET /api/feed-ads/active` |
| Admin queue filter/count | `projectFeedAdOpsProductStatus` (campaign status + window) — not raw request.status |
| Campaign end → request | `endFeedAdCampaign` + `syncFeedAdRequestEndedWithCampaign` |
| Admin ops badge | `/api/admin/admin-bell` `feed_ad_requests` pending_review COUNT |
