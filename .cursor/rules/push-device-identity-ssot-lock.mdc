---
description: Push Device Identity SSOT LOCK — one active FCM per user, register/deactivate/dispatch (2026-06-30)
globs:
  - app/api/me/devices/register/route.ts
  - app/api/me/devices/deactivate/route.ts
  - lib/push/device-register/**
  - lib/push/dispatch/load-active-push-targets.ts
  - lib/push/dispatch/filter-user-device-push-targets.ts
  - components/push/NativePushRegistration.tsx
  - lib/permissions/dibay-post-login-onboarding-gate.ts
  - components/permissions/DiBaYDevicePermissionOnboardingGate.tsx
  - lib/push/disconnect-native-devices-for-logout-client.ts
  - lib/push/native/register-native-push-client.ts
  - docs/push/push-device-identity-ssot-lock.md
alwaysApply: false
---

# Push Device Identity SSOT LOCK

Full doc: `docs/push/push-device-identity-ssot-lock.md`  
**Status:** LOCK (2026-06-30) — P0-1 + P0-2 PASS on prod QA device `RRGL4046NTW`

## Fixed invariant

- **Chat / non-call:** one FCM target per user at dispatch (`single_fcm`).
- **Call multi-device (2026-07-29):** `incoming_call` / terminal / `call_answered_elsewhere` fan-out one FCM per `device_id`. Register **must not** wipe other devices' FCM. See `docs/dibay-call-multi-device-policy.md`.
- Register **must not** hard-depend on notification onboarding UI settled (P0-1).
- Legacy `dibay-*` `device_id` **must not** re-activate when fresher UUID FCM peer exists (30d).

## Without explicit user approval — DO NOT

1. Modify Native Call LOCK files unrelated to multi-device call policy, or DB `user_devices` schema for chat identity alone.
2. Start **P3-2** (MissedCallNotificationHelper, APNS, admin push UI, P3-2 scope).
3. Revert call multi-device FCM fan-out or restore user-wide FCM deactivate on register without product approval.
4. Re-block register on `waitForNotificationOnboardingSettled()`.
5. Touch notification sound SSOT LOCK (`docs/notifications/notification-sound-ssot-phase1-lock.md`).

## Regression gate

```bash
vitest run lib/push/device-register/__tests__/device-register-gate.test.ts \
  lib/push/device-register/__tests__/should-activate-fcm-device-register.test.ts \
  lib/push/dispatch/__tests__/filter-user-device-push-targets.test.ts
npm run verify:push-dispatch-contract
```

Device QA: `docs/push/push-device-identity-ssot-lock.md` §7.
