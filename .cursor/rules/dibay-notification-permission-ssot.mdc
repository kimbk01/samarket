---
description: DIBAY Notification Permission SSOT — OS-first, single request path, Incoming/Push gate
globs:
  - lib/permissions/permission-manager/**
  - lib/push/native/**
  - components/permissions/**
  - components/push/**
  - android/app/src/main/java/com/dibay/app/IncomingCallPushDelivery.java
  - android/app/src/main/java/com/dibay/app/NotificationReceiveGate.java
  - android/app/src/main/java/com/dibay/app/IncomingCallNotificationBuilder.java
  - android/app/src/main/java/com/dibay/app/NativeDevicePermissionsPlugin.java
alwaysApply: false
---

# DIBAY Notification Permission SSOT LOCK

Full doc: `docs/permissions/dibay-notification-permission-ssot.lock.md`

## Single request path

OS notification request (`POST_NOTIFICATIONS`, `PushNotifications.requestPermissions`, `Notification.requestPermission`) **only** inside:

- `lib/permissions/permission-manager/adapters/*.ts` via `requestNotificationFromGuide()`

## OS-first (permission UX)

- First login / settings_retry when `canRequestOsNotificationPrompt`: **direct** `requestNotificationFromGuide()` — **no** app guide modal before OS.
- `disabled_resume` / visibility: sync only — **no** app modal, **no** OS reprompt.
- `PERMANENT_DENIED` / `SYSTEM_DISABLED`: settings-only fallback modal (`NotificationGuideModalHost`) on explicit `settings_retry` only — **no** OS popup repeat.

## Composite tiers (LOCK)

### receiveReady (Push + FCM Runtime)

- POST_NOTIFICATIONS granted
- app notifications enabled
- incoming channel enabled (v7 OR native_voice OR native_video)
- NOT appBlocked (JS flag)

**FSI and battery are NOT in receiveReady.**

### lockScreenIncomingReady (lock FSI / Activity / fallback)

- receiveReady AND fullScreenIntentAllowed AND battery not restricted

FSI OFF: Push/FCM **not** blocked when receiveReady=true. Lock-screen FSI, Activity fallback **blocked**.

## Gates

| Path | Gate |
|------|------|
| Push Register | `ensureNotificationForPushRegister()` — check only |
| FCM Runtime | `NotificationReceiveGate.receiveReady` before `handleIncoming` |
| Lock FSI / Activity fallback | `NotificationReceiveGate.lockScreenIncomingReady` in `IncomingCallNotificationBuilder` |

## DO NOT

1. Modify `NativeVoiceCallRuntime` / `NativeVideoCallRuntime` for notification gate
2. Add notification OS request outside PermissionManager adapters
3. Register push token when `!receiveReady`
4. Call Runtime when `!receiveReady`
5. Call `launchActivityFallback` or attach FSI when `!lockScreenIncomingReady`
6. Put FSI or battery into `receiveReady` formula

## After change

```bash
npm run verify:notification-permission-ssot-contract
node scripts/qa/notification-receive-gate-adb-qa.mjs
```

## Legacy Receive Policy (3-tier — do not mix)

Full doc: `docs/permissions/dibay-legacy-call-receive-policy.md`

- **DIBAY only:** Runtime official gate = `receiveReady` (Notification). Do **not** state this as universal legacy-app law.
- **Runtime tier:** Notification → FCM → Runtime → Ring → Incoming UI. FSI/mic/camera/battery/speaker/bluetooth **must not** block Runtime.
- **Lock Screen tier:** FSI/battery → lock full UI only; Runtime/Ring/Incoming UI **continue**.
- **Media tier:** Mic/Camera at accept/outgoing gesture → Join only; **must not** block receive.
- **Invariant:** Permission UX changes **must not** modify Runtime, FCM, Agora, Native Runtime, `NotificationReceiveGate`.
- Post-login FSI (Android): `runPostLoginFullScreenIntentCheck()` — OS settings direct, no DIBAY modal.
