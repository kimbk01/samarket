import { AdminPromotionLandingClient } from "@/components/admin/platform-promotion/AdminPromotionLandingClient";

/** Discoverability hub — not a metrics dashboard. */
export default function AdminPromotionLandingPage() {
  return <AdminPromotionLandingClient />;
}
