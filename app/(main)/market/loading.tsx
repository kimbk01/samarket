/**
 * `/market` 세그먼트 — Next `loading.tsx` 가 스켈레톤을 덮으면 1차 탭 전환 체감이 440ms 느려진다.
 * 본문은 `MarketCategoryRouteFallback`·클라 피드 캐시가 채운다.
 */
export default function MarketSegmentLoading() {
  return null;
}
