import { AddressSelectRoute } from "./address-select-route";

/** Google Maps 기반 위치 선택 — `sessionStorage`로 이전 화면에 좌표·주소 전달 */
export default function AddressSelectPage() {
  return <AddressSelectRoute />;
}
