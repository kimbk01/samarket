import { MainHomeShellLoading } from "@/components/layout/MainRouteLoading";

/** `(main)` 기본 — 세그먼트 전용 `loading.tsx` 가 없을 때 */
export default function MainLoading() {
  return <MainHomeShellLoading />;
}
