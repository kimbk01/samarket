import { CallQaDebugPanel } from "@/components/debug/CallQaDebugPanel";

/** P4 APK distributed QA — in-app log viewer (Device B, no USB logcat). */
export default function CallQaDebugPage() {
  return <CallQaDebugPanel />;
}
