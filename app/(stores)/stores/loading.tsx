import { MainFeedRouteLoading } from "@/components/layout/MainRouteLoading";

/** `/stores` 매장 허브 — `(stores)` route group */
export default function StoresHubLoading() {
  return <MainFeedRouteLoading rows={5} />;
}
