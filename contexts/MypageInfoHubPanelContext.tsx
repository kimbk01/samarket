"use client";

import type { ReactNode } from "react";

/** @deprecated 햄버거 사이드 패널 제거 — pass-through only. */
export function MypageInfoHubPanelProvider({ children }: { children: ReactNode }) {
  return children;
}
