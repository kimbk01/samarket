# Bell Domain Projection Authority — CODE PASS note

**Commit tip (pre-push):** pending  
**Base:** `8dab86490`

## Product lock

- Header Bell SSOT = `buildNotificationBadgeProjection` → `applyNotificationBadgeProjection` → badge-count store
- `notification_events` SUM = inbox `categoryCounts` only
- App Icon = independent `appIconTotal` (no `bell_mirror`)
- Bottom Chat = GD + group room count only
- Trade / SO in Bell, not Bottom

## Verdict (code)

`CODE PASS — BELL DOMAIN RUNTIME VERIFICATION PENDING`

Overall messenger: `PASS — TRADE LIST PRODUCTION VERIFIED / FOUR-DOMAIN INFRASTRUCTURE PENDING`

Production device QA (scenarios 1–7 ×3) still required for:

`PASS — BELL DOMAIN PROJECTION AUTHORITY VERIFIED`
