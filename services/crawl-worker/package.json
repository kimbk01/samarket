{
  "name": "@dibay/crawl-worker",
  "private": true,
  "type": "module",
  "description": "DIBAY external-import crawl worker — Crawlee foundation (not Vercel Functions)",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "boot": "npx tsx boot.mjs",
    "poc": "npx tsx run-poc.mjs",
    "job": "npx tsx run-job.mjs",
    "loop": "npx tsx worker-loop.mjs"
  }
}
