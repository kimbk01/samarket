# 거래·체감 속도 핫패스 — 수정 이력 (append-only)

> **목적**: 속도·진입 경로를 바꿀 때 **무엇이 개선된 상태인지** 남겨, 이후 작업이 **의도 없이 예전(느린) 구조로 되돌아가는 것**을 줄인다.  
> **원칙**: 이 파일은 **삭제하지 않고**, 새 이력은 **항상 아래에 추가**한다. 예전 항목을 고치지 않는다(잘못 적었으면 새 행으로 정정 사유를 남긴다).

**연계 규칙**

- 절차: `.cursor/rules/samarket-perf-change-protocol.mdc`
- 계약: `.cursor/rules/trade-post-detail-chat-hot-path.mdc`, `.cursor/rules/chat-detail-bottom-nav-authority.mdc`
- 자동 검증: `npm run verify:trade-hot-path-contract`

---

## 이력 추가 방법 (PR·커밋 시 필수)

아래 **거래 핫패스·메신저 탭·RSC 분리** 중 하나를 바꾸면, **같은 PR(또는 연속 커밋)** 에서 반드시 표를 한 줄 추가한다.

1. **날짜**: `YYYY-MM-DD` (작업일)
2. **영역**: 예) 상세 RSC / 채팅 진입 / 목록 prefetch / 메신저 탭
3. **변경 요약**: 한 줄로 “무엇을 개선 상태로 고정했는지”
4. **주요 파일**: 쉼표 구분 경로
5. **검증**: 실행한 명령·수동 확인 (예: `verify:trade-hot-path-contract`, `tsc`)
6. **되돌림 시**: 되돌리려면 규칙·주석·스크립트를 **함께** 갱신해야 함을 적음

**역행 금지**: 이력 없이 계약만 지우거나, 검증 스크립트 금지 목록만 삭제해 통과시키지 말 것.

---

## 이력

| 날짜 | 영역 | 변경 요약 (개선으로 고정된 상태) | 주요 파일 | 검증 | 되돌림 시 |
|------|------|----------------------------------|-----------|------|-----------|
| 2026-05-03 | 메신저 탭 | 채팅 상세(`isChatRoomDetail`)에서 메인 BottomNav 비표시를 **경로 플래그 단일 권한**으로 고정. store·키보드로 탭을 다시 조종하지 않음 | `conditional-app-shell-flags.ts`, `ConditionalAppShell.tsx`, `useMessengerUIStore.ts`(플래그 제거), `CommunityMessengerRoomPhase2.tsx` | 수동·레이아웃 확인 | `chat-detail-bottom-nav-authority.mdc` 및 플래그 주석과 함께 제품 결정 필요 |
| 2026-05-03 | 채팅 진입 | 신규 거래 채팅: 상세에서 **동기 방 생성 대기 제거**, 즉시 `compose` 로 `replace` → `TradeChatComposeClient` 에서 확정 | `trade-chat-entry-navigation.ts`, `TradeChatComposeClient.tsx` | 수동 플로우 | `trade-post-detail-chat-hot-path.mdc`·모듈 CONTRACT 주석과 함께 변경 |
| 2026-05-03 | 상세 RSC | `getItemDetailPageData` **첫 응답에서 related·거래방 시드·판매자 제안 시드 제외**. related 는 `GET /api/posts/[id]/related` 만 사용 | `trade-detail.service.ts`, `PostDetailPageClient.tsx`, `app/api/posts/[postId]/related/route.ts` | `npm run verify:trade-hot-path-contract`, `tsc` | 검증 스크립트·계약 주석 제거 없이 RSC에 다시 합치지 말 것 |
| 2026-05-03 | 목록→상세 | 홈 거래 목록 상단 카드 **`/post/:id` idle prefetch** 로 상세 RSC 선행 | `HomeProductList.tsx` | 수동 | `trade-post-detail-chat-hot-path.mdc` 참고 후 조정 |
| 2026-05-03 | 상세→채팅 예열 | 상세 마운트·짧은 호버에서 `prefetchTradeChatEntry(..., prepareIfCreate)` 강화 | `PostDetailView.tsx` | 수동 | 계약 문서에 맞춰 조정 |
| 2026-05-03 | 운영 절차 | 역행 방지: **`samarket-perf-change-protocol.mdc`**(항시), **`verify:trade-hot-path-contract`**, `npm run check` 포함 | `.cursor/rules/*.mdc`, `scripts/verify-trade-hot-path-contract.cjs`, `package.json` | `npm run verify:trade-hot-path-contract` | 스크립트 완화 시 본 changelog 에 **사유 행** 추가 |
| 2026-05-03 | 상세 related UI | **유사·판매자 물품 미표시 수정**: related 클라 fetch 가 `runSingleFlight`+Strict Mode 에서 `setRelated` 영구 스킵되던 버그 → `AbortController` 로 교체 (하단 메뉴와 무관) | `PostDetailPageClient.tsx`, `trade-post-detail-chat-hot-path.mdc` | `tsc`, 수동 상세 확인 | 다시 `runSingleFlight`+cancelled 패턴으로 되돌리지 말 것 |
| 2026-05-03 | 상세 related 신뢰성 | 클라 `/api/.../related` 단독 의존 제거 → **`getItemDetailPageData` 가 `getTradeDetailRelatedData` 를 프로필·제안과 병렬 await** 해 RSC 번들에 실음 (유사·판매자 물품 복구) | `trade-detail.service.ts`, `PostDetailPageClient.tsx`, `trade-post-detail-chat-hot-path.mdc` | `verify:trade-hot-path-contract`, `tsc` | 상세 첫 화면을 클라 related 단독으로만 두지 말 것 |
| 2026-05-03 | 메신저 탭 | **`GET …/home-sync?tier=critical`** 방 목록 조립 시 프로필을 **`hydrateProfilesLabelsOnly`** 로만 채움(`getViewerRelationSets` 3쿼리 생략). **`tier=full`**·부트스트랩 전체 경로는 기존 **`hydrateProfiles`** 유지 | `lib/community-messenger/service.ts` (`listCommunityMessengerMyChatsAndGroups`) | `npx tsc --noEmit`, 수동 Network `home-sync` critical | critical 단계에서 관계 플래그가 비어 있으면 full 동기 후 정상; 되돌림 시 changelog·의도 명시 |
| 2026-05-04 | 채팅 진입 | 거래 방 URL이 **메신저 방 UUID** 일 때 `warmChatRoomEntryById`(레거시 `/api/chat/room/…/bootstrap`)만 웜업되던 간극을 줄이기 위해 **`prefetchCommunityMessengerRoomSnapshot(cmRoomId)`** 를 compose 확정·기존방 진입·목록 prefetch 에 추가해 `/api/community-messenger/rooms/…/bootstrap` 리스트 모드 캐시를 선채움 | `TradeChatComposeClient.tsx`, `trade-chat-entry-navigation.ts`, `room-snapshot-cache.ts`(기존 API 재사용) | `npm run verify:trade-hot-path-contract`, `npx tsc --noEmit` | 메신저 방 진입이 다시 콜드 스타트만 두지 말 것 |
| 2026-05-04 | 채팅 진입(resolve) | **`resolveTradeChatEntry` 가 동일 프로세스 안에서 `POST /api/chat/item/start` 를 다시 HTTP 호출하던 병목 제거** — `runItemTradeChatStartCore` 단일 코어 공유. **`TRADE_ENTRY_PERF_LOG=1`** 일 때 단계별 ms 로그(`trade-entry-perf-log.ts`). 측정 절차: `docs/trade-entry-resolve-perf.md` | `item-trade-chat-start-core.ts`, `trade-chat-entry-resolve.ts`, `app/api/chat/item/start/route.ts`, `app/api/trade/chat/entry/resolve/route.ts`, `trade-entry-perf-log.ts` | `verify:trade-hot-path-contract`, `tsc`, 수동 Network 3회 + 서버 `_total_ms` 대조 | 예전처럼 resolve 안에서 내부 `fetch(item/start)` 를 되살리지 말 것 |
| 2026-05-04 | 거래 표면 정합 | 글쓰기 런처 **거래 루트**를 `/market` 홈칩과 동일 소스(`getHomeChipCategories` ∩ `can_write`)로 통일. 거래 채팅 목록 `contextMeta`에 **`categoryMenuLabel`** 보강·1행 표시 | `getWritableRootCategoriesForWriteLauncher.ts`, `service.ts`, `view-model.ts`, `TradeChatListRowContent.tsx`, `category-menu-label.ts`, `community-messenger-trade-chat-list.md` | `verify:trade-hot-path-contract`, `npx tsc --noEmit`, `npm run verify:messenger-home`, vitest 해당 파일 | 런처만 `quick_create` 로 마켓 탭과 갈라지게 되돌리지 말 것 |
| 2026-05-05 | 거래 채팅 목록 | 1행 칩이 대메뉴만 보이던 문제를 **`productCategoryLabel`**(게시글 `trade_category_id` 의 카테고리 `name`) 우선 표시로 해소. `headline` 은 `title` 공백 시 `meta.listing_title` 등 보조 | `types.ts`, `room-context-meta.ts`, `product-chat-messenger-meta.ts`, `service.ts`, `trade-post-row-fields.ts`, `view-model.ts`, `TradeChatListRowContent.tsx`, `community-messenger-trade-chat-list.md` | `verify:trade-hot-path-contract`, `npx tsc --noEmit`, vitest 해당 모듈 | leaf 라벨 없이 대메뉴만 두고 세부 구분을 숨기지 말 것 |
| 2026-05-05 | 거래 채팅 목록 | **`useTradeChatListMetaHydration`**: 썸네일이 이미 있어도 `postId` 있는데 **`productCategoryLabel` 비면** `trade-chat-list-meta` 를 **방당 1회** 호출(구 캐시·옛 부트스트랩 보강). 거래 방만 `communityMessengerRoomIsTrade` 로 한정 | `use-trade-chat-list-meta-hydration.ts` | `tsc` | 썸네일만 조건으로 leaf 보강을 영구 스킵하지 말 것 |
| 2026-05-05 | 거래 채팅 목록 | **근본**: `sortRoomsWithStableOutput`·`visibleChatListInputKey` 정렬/가시 캐시 키가 **`thumbnailUrl`만** 포함해 `contextMeta` 보강 후에도 **옛 `chats` 행 참조가 캐시 히트**로 남음 → `messengerRoomTradeListMetaSig` 로 headline·leaf 라벨 등을 키·행 동결에 포함 | `use-community-messenger-home-state.ts` | `tsc` | 거래 목록 캐시 키에서 trade 메타 시그니처를 빼면 동일 증상 재발 |
| 2026-05-05 | 거래 채팅 목록 | **근본(2)**: `home-sync` `critical_patch` 가 `mergeCriticalRoomPatchesIntoLists` 로 상단 방을 **통째 교체**하면서 서버 페이로드가 `contextMeta` 를 빼거나 `headline:"거래"` 만 주면 **클라 보강분이 덮어써져 소거**됨 → `mergeMessengerRoomSummaryForHomeSyncCriticalPatch` 로 이전 trade 메타 우선 유지 | `merge-critical-home-sync-room-summary.ts`, `use-community-messenger-home-bootstrap.ts` | `tsc`, vitest `merge-critical-home-sync-room-summary.test.ts` | critical 병합 시 trade 메타를 무조건 incoming 만 믿지 말 것 |
| 2026-05-05 | 거래 채팅 목록 | 목록 2행 제목·가격: `contextMeta` 가 `"거래"` 등일 때 **`GET /api/community-messenger/trade-post-list-preview?postId=`** 로 `posts` 제목·가격 확정 + `useTradeChatListPostPreviewFields` 클라 캐시 | `trade-post-list-preview/route.ts`, `use-trade-chat-list-post-preview-fields.ts`, `MessengerChatListItem.tsx`, `TradeChatListRowContent.tsx` | `tsc`, vitest, `verify:trade-hot-path-contract` | 인증 없이 postId 열람 API 추가 금지 |
| 2026-05-04 | 거래 채팅 목록 | **4행 UI**: 1행 칩은 **`categoryMenuLabel`(5대 메뉴)만**; 4행은 **`sellerDisplayName`**(`enrichTradeRoomContextMetaForBootstrap` 마지막 배치 — `product_chats.seller_id` 우선·없으면 `posts.user_id` + `fetchProfilesByIds`). 삭제 글은 preview **404** 시 제목 **「삭제된 거래글」**. 정렬 캐시 시그니처에 `sellerDisplayName` 포함 | `types.ts`, `room-context-meta.ts`, `product-chat-messenger-meta.ts`, `merge-critical-home-sync-room-summary.ts`, `service.ts`, `view-model.ts`, `use-trade-chat-list-post-preview-fields.ts`, `TradeChatListRowContent.tsx`, `MessengerChatListItem.tsx`, `use-community-messenger-home-state.ts`, `community-messenger-trade-chat-list.md` | `verify:trade-hot-path-contract`, `npx tsc --noEmit` | 목록 enrich 호출 순서·tier·입장 경로를 바꾸지 말 것; `critical_patch` 병합 시 `sellerDisplayName` 역행 방지 유지 |
| 2026-05-05 | 거래 채팅 목록 | **연결·enrich**: `item_trade` 경로에서 **`product_chats.community_messenger_room_id` 미저장**이면 Phase B 조인 실패 → **항상** `persistProductChatMessengerRoomId`. 썸네일만 있고 headline `"거래"`·`postId` 비면 **`product_chats`→`posts` 재보강**. 브리지가 **`summary` 를 카테고리 없는 메타로 덮어쓰던 것** 제거 | `service.ts` (`ensureCommunityMessengerDirectRoomFromProductChat`, `enrichTradeRoomContextMetaForBootstrap`), `app/api/community-messenger/bridge/product-chat/route.ts`, `MessengerChatListItem.tsx`(dev 로그), `community-messenger-trade-room-post-linkage.md` | `verify:trade-hot-path-contract`, `npx tsc --noEmit` | `trade_post_id` 컬럼 추가·unread/bootstrap 변경 금지 |
| 2026-05-05 | 거래 채팅 생성 | **`product_chats` 원장 고정**: `runItemTradeChatStartCore` 가 메신저 링크를 **`after()` 만** 돌리던 것을 **`await ensureMessengerRoomIdForItemTrade`** 로 응답 전 완료로 바꿈. `chat_rooms` 에만 CM id 있을 때 **`persistProductChatMessengerRoomIdIfNull`** 로 PC 보강 | `item-trade-chat-start-core.ts`, `persist-trade-messenger-room-link.ts`, `ensure-messenger-room-for-trade-chat.ts` | `verify:trade-hot-path-contract`, `tsc` | resolve 응답 직후 목록이 비지 않게 할 것 |
| 2026-05-09 | 메신저 탭 | **home-sync trade enrich 브리지**: Phase B `product_chats`(cm_room)·Phase C ledger(`chat_rooms`) 조회를 **Promise.all** 로 겹침·seed `product_chats` 에 **`community_messenger_room_id`** 로 Phase B DB 왕복 축소. **`tradePcBridgeQueriesMs`** = B+C 병렬 벽시계(`phaseBcLedgerParallelWallMs`) + CC + D | `lib/community-messenger/service.ts`, `lib/community-messenger/home-sync-trace.ts` | `npx tsc --noEmit` | ledger 선조회는 Phase B 전「썸네일 미비」방 상한; 적용 시 post-B `stillNeedThumb` 로 필터(역행 시 순차 이중 조회로 되돌아감) |
| 2026-05-09 | 거래 채팅 목록 | **`GET …/trade-post-list-preview`**: 운영·로컬 일부 스키마에 **`posts.currency` 미존재** 시 PostgREST 500 방지 — `currency` 포함 SELECT 실패 시 **`id,title,price,meta`** 로 재시도·통화는 기존처럼 **PHP 폴백** | `app/api/community-messenger/trade-post-list-preview/route.ts` | `npx tsc --noEmit` | 마이그레이션으로 `currency` 추가 후에도 폴백 유지 권장 |
| 2026-05-09 | 거래 채팅 목록 | **`trade-post-list-preview` 보강**: Supabase 에러 **`message/details/hint`·코드 `42703`** 로 재시도 판정·**`meta` 미존재** 시 **`id,title,price`** 3단계·중복 `not_found` 분기 제거 | `app/api/community-messenger/trade-post-list-preview/route.ts` | `npx tsc --noEmit` | 동일 |
| 2026-05-09 | 거래 채팅 목록 | **`trade-post-list-preview`**: **`posts.currency` SELECT 제거** — 레거시 DB·PostgREST에서 컬럼 누락 500 차단·통화는 **`meta.currency` 또는 PHP** | `app/api/community-messenger/trade-post-list-preview/route.ts` | `npx tsc --noEmit` | 목록 프리뷰에서 DB 통화 컬럼 의존 되살리지 말 것 |
| 2026-05-09 | 거래 채팅 목록 | **`trade-post-list-preview` 근본**: **`POSTS_TABLE_READ`(posts_masked) 미사용** — 마스킹 뷰가 과거 컬럼(`currency` 등)을 고정 참조하면 컬럼 드롭 후 뷰만 깨져 동일 에러 반복 → 서비스 롤로 **`posts` 실테이블**만 조회 | `app/api/community-messenger/trade-post-list-preview/route.ts` | `npx tsc --noEmit` | 마스킹이 필요한 클라 직접 조회는 `posts_masked` 재생성 마이그레이션으로 정합 유지 |
| 2026-05-10 | 메신저 탭 | **HS5 home-sync unread**: 거래 레거시 미읽음 소스 `chat_rooms`·`product_chats` 조회를 **직렬 → Promise.all 병렬**로 바꿔 `unreadSourceFetchMs`를 **벽시계(병렬 구간)** 기준으로 계측. trace: `unreadParallelWallMs`, `unreadEffectiveRttCount`(세그먼트 1). critical에서 목표 미달 시 **`[home-sync-fail] HS5 unread target missed`** | `lib/community-messenger/enrich-messenger-trade-unread-with-legacy-trade.ts`, `lib/community-messenger/home-sync-trace.ts`, `app/api/community-messenger/home-sync/route.ts` | `npx tsc --noEmit`, 수동 `GET …/home-sync?tier=critical` warm 3회 | 의미론·합산 공식(링크 시 CM만·미링크 시 max(cm, PC)) 불변; 직렬 이중 RTT로 되돌리면 wall 목표 재악화 |
| 2026-05-10 | 메신저 탭 | **HS5-RPC-DEEP 관측**: `home_sync_hs5_unread_legacy_bundle` 반환에 **`_hs5RpcDebug`** (서버 `clock_timestamp` 구간: chat/product/merge/json/total·행 수). TS trace·`[home-sync-fail] HS5 rpc bundle tail breakdown` 연결. 의미·폴백 불변 | `supabase/migrations/20260510203000_home_sync_hs5_unread_legacy_bundle_rpc_intrinsic_debug.sql`, `lib/community-messenger/enrich-messenger-trade-unread-with-legacy-trade.ts`, `lib/community-messenger/home-sync-trace.ts`, `app/api/community-messenger/home-sync/route.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | `_hs5RpcDebug` 제거 시 tail 원인 분해 불가 |
| 2026-05-10 | 메신저 탭 | **critical bootstrap 관측만**: `tier=critical` 응답 **`x-samarket-critical-payload-kb`·`route-ms`·`serialization-ms`·`room-count`** 헤더; 클라 홈 부트스트랩에서 **`[critical-bootstrap-client]`** JSON(`critical_fetch_ms`·`critical_json_parse_ms`·`critical_state_apply_ms`·`critical_first_list_visible_ms`·`critical_render_commit_ms`·서버 ms). 페이로드·HS·unread 의미·shell/deferred 순서 불변 | `app/api/community-messenger/bootstrap/route.ts`, `lib/community-messenger/home/use-community-messenger-home-bootstrap.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | 헤더/로그 제거 시 `messenger_critical_done_ms` 벽시계 분해 불가 |
| 2026-05-10 | 메신저 탭 | **critical bootstrap DB/CPU**: `tier=critical` 전용으로 **`fetchMyRoomsPayload`의 room_profiles 라운드 생략**(`includeRoomProfiles:false`), **`byRoomId` 재사용**·**그룹 참가자 슬라이스 1회/방**·`profilesMs`를 **라벨 hydrate만**으로 분리; `[cm-bootstrap-v2]`에 **`critical_*`·`critical_cpu_merge_ms`**(summarize+mapRows CPU) 추가. HS3/4/5·unread 합산·realtime/silent delta·shell·deferred 미변경 | `lib/community-messenger/bootstrap/critical-stage.ts`, `lib/community-messenger/service.ts`, `app/api/community-messenger/bootstrap/route.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | room_profiles 라운드 복구 시 critical RTT+1; UI 별칭 표면이 room-scoped와 어긋나면 제품 확인 후 조정 |
| 2026-05-10 | 메신저 탭 | **Supabase refresh 경쟁**: `GET /api/auth/session` 을 **`getUser()`→`getSession()`** 으로 바꿔 서버 측 refresh 유발 제거; `SessionLostRedirect` 의 `refreshSession()` 은 **`runBrowserAuthRefreshDeduped`** 단일 비행. 클라 `[auth_client_created]`·`[auth_session_changed]`·`[auth_refresh_*]` 로그. bootstrap 페이로드·shell·HS 불변 | `app/api/auth/session/route.ts`, `lib/supabase/client.ts`, `lib/supabase/auth-refresh-telemetry.ts`, `components/auth/SessionLostRedirect.tsx`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | 세션 API를 다시 `getUser()` 중심으로 되돌리면 `/api/auth/session`·브라우저 auto-refresh 와 refresh token 경쟁 재발 |
| 2026-05-10 | 메신저 탭 | **fetch abort·세션 401 완화**: `fetchAuthSessionNoStore` 에 **401 1회 재시도**·`TypeError: Failed to fetch` **1회 재시도**; `[fetch_client]` JSON(`fetch_abort_*`·`auth_401_*`·`stale_response_ignored`·`fetch_network_retry`). 메신저 `refresh` catch 에서 **abort 계열 조기 return**·critical 직후 **stale 응답 무시 로그**. `SupabaseAuthSync` **hydrate Promise 미처리 rejection 방지**. APP-Shell·bootstrap·HS·UI 불변 | `lib/auth/fetch-auth-session-client.ts`, `lib/http/fetch-client-telemetry.ts`, `lib/community-messenger/home/use-community-messenger-home-bootstrap.ts`, `components/auth/SupabaseAuthSync.tsx`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | 401 재시도 제거 시 `SessionLostRedirect`·콘솔 red 빈도 재증가 가능 |
| 2026-05-10 | 개발 콘솔 | **`[nav-perf]`·`[community-feed:perf-diag]`·`[samarket-runtime-debug:phase]` 기본 off**: `localStorage`/`NEXT_PUBLIC_*` 게이트(`lib/debug/samarket-client-console-flags.ts`); nav 로그는 `console.debug`. **`GET /api/me/notifications?unread_count_only=1` 비로그인 → 200 `{ ok, unread_count:0 }`**(세션 전 폴링 노이즈). **`/api/philife/neighborhood-feed` 병목 후보**만 기록(코드·SQL 미변경) | `lib/navigation/nav-perf-browser.ts`, `contexts/LatestMenuNavigationContext.tsx`, `components/community/CommunityFeed.tsx`, `lib/runtime/samarket-runtime-debug.ts`, `app/api/me/notifications/route.ts`, `tests/e2e/nav-perf-idle-bottom-nav.spec.ts`, `tests/e2e/philife-community-perf-breakdown.spec.ts` | `npx tsc --noEmit` | E2E 측정 시 각각 `samarket:debug:navPerf` / `samarket:debug:philifeFeedPerf` 필요 |
| 2026-05-10 | 필라이프 API | **`GET …/neighborhood-feed` 앱 레이어 분해 로그** — `[philife-feed-api-steps]`(`total_ms`·`auth_ms`·`query_ms`·`normalize_ms`·`enrich_ms`·`serialize_ms`·`payload_kb`·`result_count`·`duplicate_window_count`); 헤더 `x-samarket-community-feed-perf` 에 `philife_feed_*`·`philife_feed_steps_ms` 추가. **1500ms 동일 URL 완료 횟수** in-process(`neighborhood-feed-duplicate-window.ts`). SQL/RPC·UI·shell·메신저 미변경 | `app/api/community/neighborhood-feed/route.ts`, `lib/neighborhood/neighborhood-feed-duplicate-window.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit`, 로컬 3회 GET 샘플 | 관측만; 단계 합이 `total` 과 어긋나면 `topics_resolve`·Next 직렬화 등 라우트 상단 구간 별도 확인 |
| 2026-05-10 | 필라이프 API | **neighborhood-feed 단기 중복 제거**: 서버 `runNeighborhoodFeedWithShortTtl`(in-flight `runSingleFlight` + **완료 후 1200ms TTL** 동일 `cacheKey` 재사용, `recordNeighborhoodFeedCompletion` 은 **네트워크 1회**만); 클라 `fetchNeighborhoodFeedShortTtl` 동일 패턴. TTL·in-flight 응답은 **동일 JSON**·`philife_feed_short_ttl_served` 헤더(개발). 로그 `[philife-feed-short-ttl]`·클라 메트릭. SQL/RPC·피드 결과·UI·shell·메신저 미변경 | `lib/neighborhood/neighborhood-feed-short-ttl-server.ts`, `lib/philife/fetch-neighborhood-feed-short-ttl.ts`, `app/api/community/neighborhood-feed/route.ts`, `components/community/CommunityFeed.tsx`, `lib/philife/warm-philife-neighborhood-feed.ts`, `lib/main-menu/bottom-nav-tap-prewarm-data.ts`, `lib/community/fetch-community-my-hub-posts-deduped.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit`, curl 3연속 | TTL 키는 `viewerId::정규화URL`; 권한·쿼리 변경 시 TTL 키·무효화 정책 재검토 |
| 2026-05-10 | 메신저 탭 | **home-sync 병목 한 줄 로그**: 개발 또는 `SAMARKET_LOG_HOME_SYNC_BREAKDOWN=1` 시 **`[home-sync-breakdown]`**(`total_ms`·`rooms_ms`·`unread_ms`·`profiles_ms`·`trade_ms`·`serialize_ms`·`payload_kb`·`duplicate_window_count`·`short_ttl_hit`·`routeBundleAwaitMs` 등). **dev full tier**에도 `trace.token`을 켜 bundleSteps·unread 세분 계측. **1500ms** 동일 사용자·URL 완료 횟수(`home-sync-duplicate-window.ts`). 페이로드·HS·unread 의미·캐시 TTL·bootstrap·shell·UI 불변 | `app/api/community-messenger/home-sync/route.ts`, `lib/community-messenger/home-sync-duplicate-window.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | prod full 은 trace 미전파로 세부 ms 는 0/null 가능; 관측만 |
| 2026-05-13 | 메신저 탭 | **`[home-sync-deep-trace]`** — dev 또는 `SAMARKET_LOG_HOME_SYNC_DEEP_TRACE=1` 이고 `trace` 있을 때 단일 로그로 `home_sync_*` ms·unread 분해·`top_home_sync_bottleneck*`·in-process 캐시 lookup/store·`heap_used_mb`/`rss_mb`·webpack/realtime **서버 비관측** 안내. `duplicate_window_count` 는 성공 응답 직전 항상 기록. `homeSyncTraceMeterEnabled` 시 `fetchMyRoomsPayload` 진단 항상 수집·`roomsRound2RoomsDbFetchMs` 를 bundleSteps·TTL 스냅샷에 포함. 페이로드·unread 정합·UI·Realtime 클라 계약 불변 | `lib/community-messenger/home-sync-deep-trace-log.ts`, `app/api/community-messenger/home-sync/route.ts`, `lib/community-messenger/home-sync-trace.ts`, `lib/community-messenger/service.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | `home_sync_postgrest_wait_ms` 는 rooms+unread 벽시계 합(병렬 max 아님); `realtime_*` 는 브라우저 구독 상태로 서버 0 |
| 2026-05-13 | 메신저 탭 | **home-sync dev heap**: `node:v8` **`getHeapStatistics` 를 dev 진단 구간에서만 `import()`** — 라우트 모듈 정적 그래프에서 `v8` 제거(Next dev cold compile 청크 비용 완화 목적). `[dev-heap] home-sync high heap` 동작·임계·JSON·HS·unread·응답 shape 불변 | `app/api/community-messenger/home-sync/route.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | cold compile ms 감소는 로컬 `GET …/home-sync` before/after 로만 판정 |
| 2026-05-13 | 거래 채팅 API | **`POST trade-chat-list-meta` 단계 계측**: `hydrateTradeChatListContextMetaForRoomIds` 가 **`trade_chat_meta_*`**(rooms_fetch·rooms/participants 병렬·room_profiles·summarize·enrich·posts/category·merge·payload_build·`query_count` 근사·`top_bottleneck*`) 반환; 라우트 `[dev-api-perf]` 에 병합. `enrichTradeRoomContextMetaForBootstrap` 에 **`tier: "full"` 합성 `HomeSyncTrace`** 로 기존과 동일한 posts 폴백 체인 + deepSteps `tradeMetaEnrich` 채움(빌더 CPU 계측만 추가). dev TTL 캐시 히트 시 `trade_chat_meta_dev_ttl_cache_hit` | `lib/community-messenger/service.ts`, `lib/community-messenger/trade-chat-list-meta-route-cache.ts`, `lib/community-messenger/trade-chat-list-meta-dedupe-types.ts`, `app/api/community-messenger/trade-chat-list-meta/route.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | 응답 `{ ok, patches }` 불변; `query_count` 는 REST 왕복 근사(내부 RPC 묶음과 1:1 아님) |
| 2026-05-13 | 거래 채팅 API | **`load-chat-room-detail` 정적 그래프 축소**: `apply-buyer-auto-confirm`·`apply-product-chat-time-transitions`·`buyer-review-flag`·`store-order-chat-db`·`store-order-process-criteria`·`fetchItemTradeAdminSuspended` 는 **해당 분기/백그라운드에서만 `import()`**; `reserved_buyer_id` 필드는 **`reserved-item-chat` 와 동일 규칙의 로컬 헬퍼**로 처리해 `lib/trade/reserved-item-chat` 정적 의존 제거. 응답 필드·DB·unread·거래/주문 분기 의미 불변 | `lib/chats/server/load-chat-room-detail.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | `GET /api/chat/room/[roomId]` cold compile before/after 는 로컬 dev 재시작 후 로그로만 판정 |
| 2026-05-10 | 메신저 탭 | **full home-sync trade enrich**: `trade_categories` select 후보를 **실제 스키마 우선**(실패 RTT 감소). **Phase A 와 Phase B/C 브리지**(`product_chats`·`chat_rooms`) **Promise.all 병렬**. posts 콜드 스키마 체인 벽시계 `schemaColdDetectWallMs`·**`[trade-enrich-breakdown]`**(`trade_query_count`·`fallback_attempt_count`·`schema_detect_ms`·`trade_posts_fetch_ms`·`trade_merge_ms`·`trade_cpu_ms`·`trade_payload_kb`·`duplicate_trade_keys`). 동일 방 메타 재할당 진단만 추가. critical bootstrap·unread·HS·UI·응답 의미 불변 | `lib/community-messenger/service.ts`, `lib/community-messenger/home-sync-trace.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | 병렬로 브리지가 먼저 끝나도 Phase B/C/D 는 기존과 동일 필터로 병합 |
| 2026-05-10 | 메신저 탭 | **home-sync 클라 재진입·abort**: silent `fetch` 에 상위 `AbortSignal` 미전달(merge 게이트는 호출측 유지)·**800ms TTL** 동일 tier 성공 응답 재사용·**`[home-sync-reentry]`** 로그(dev / `localStorage samarket:debug:homeSyncReentry=1`). APP-shell·bootstrap 훅·unread·trade 의미·UI 불변 | `lib/community-messenger/cm-home-silent-lists-fetch.ts`, `lib/community-messenger/cm-home-sync-reentry-log.ts`, `docs/messenger-realtime-policy.md`, `lib/runtime/__tests__/messenger-home-verification-counters.test.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit`, `vitest messenger-home-verification-counters` | 페이지 이탈 후에도 in-flight silent fetch 가 끝까지 가면 네트워크만 소모(merge 는 abort 시 스킵) |
| 2026-05-10 | 개발 콘솔 | **`[cm-rt-loop-diagnosis]`**: `[cm-rt-loop-summary]` 직후 dev 전용 — `trigger`·`pathname`·`fast_refresh_detected`(모듈 재평가 2회 이상 + 부트 후 15s)·`auth_changed`(refresh 종료·세션 시그널 12s)·`visibility_state`·`old_key`/`new_key`(요약 top 채널)·`had_existing_subscription`·`activeCountTop`. realtime 제거·HS·bootstrap·shell·UI 동작 불변 | `lib/community-messenger/realtime/cm-rt-loop-diagnosis.ts`, `lib/community-messenger/realtime/subscribe-with-retry.ts`, `lib/supabase/auth-refresh-telemetry.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | `fast_refresh_detected` 는 휴리스틱; idle 15s 이후 요약은 false 로 분류됨 |
| 2026-05-10 | 메신저 탭 | **홈 meta/rooms-in 물리 채널 churn 완화**: 마지막 React 리스너 이탈 시 **즉시 `removeChannel` 하지 않고** `MESSENGER_HOME_REALTIME_DEFERRED_PHYSICAL_STOP_GRACE_MS`(기본 4s, `NEXT_PUBLIC_MESSENGER_HOME_RT_DEFERRED_STOP_GRACE_MS`) 후 teardown; grace 내 재진입 시 지연 stop 취소·**물리 채널 재사용**. `useCommunityMessengerHomeRealtime` effect deps 에서 **`visibleTradeRoomCountForBind` 제거**(HS4 로그용·필터는 fingerprint만). **`[cm-rt-lifecycle-fix]`** dev 로그. unread·home-sync 페이로드·HS3/4/5·shell·UI 불변 | `lib/community-messenger/use-community-messenger-realtime.ts`, `lib/community-messenger/realtime/cm-rt-lifecycle-fix-log.ts`, `lib/community-messenger/messenger-latency-config.ts`, `docs/messenger-realtime-policy.md`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | grace 초과 후 다른 사용자로 전환 시 이전 사용자 채널은 정상 stop |
| 2026-05-10 | 메신저 탭 | **grace keepalive 정식화**: 기본 grace **6s**·env 상한 **8s**(`MESSENGER_HOME_REALTIME_DEFERRED_PHYSICAL_STOP_GRACE_MS`). **`[cm-rt-grace]`** — `defer_stop`·`cancel_stop`·`reuse_existing`(0→1 리스너 재부착만)·`final_stop`; `[cm-rt-lifecycle-fix]` 는 `create`/`reuse`만. 즉시 teardown 아님·HS·unread·shell·UI 불변 | `lib/community-messenger/messenger-latency-config.ts`, `lib/community-messenger/use-community-messenger-realtime.ts`, `lib/community-messenger/realtime/cm-rt-grace-log.ts`, `lib/community-messenger/realtime/cm-rt-lifecycle-fix-log.ts`, `docs/messenger-realtime-policy.md`, `deployment/messenger-production.env.example`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | 8s 초과 체류 시 `final_stop` 후 재진입은 정상 `create` |
| 2026-05-10 | 개발 콘솔 | **`[cm-rt-window-summary]`**: 누적 `[cm-rt-loop-summary]` 와 별도로 **최근 30s/60s** `home_create`·`final_stop`·grace `reuse`/`cancel` 카운트·`reuse_ratio`·`cancel_ratio`·`physical_stop_rate`·`churn_level`. `subscribe-with-retry` 홈 채널 create·`logCmRtGrace` 가 이벤트 적재; loop-summary 틱 + 12s 주기 emit. HS·페이로드·UI 불변 | `lib/community-messenger/realtime/cm-rt-window-metrics.ts`, `lib/community-messenger/realtime/subscribe-with-retry.ts`, `lib/community-messenger/realtime/cm-rt-grace-log.ts`, `docs/messenger-realtime-policy.md`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | `churn_level` 은 휴리스틱; 운영 판정은 raw 카운트 병행 |
| 2026-05-10 | 메신저 탭 | **`[home-sync-full-analysis]`** — `tier=full` 시 trade/rooms/unread/profiles/serialize·`full_extra_payload_ms`·`duplicate_trade_merge_count`·`full_vs_critical_gap_ms`(직전 `tier=critical` 라우트 `route_total_ms` 대비, in-process 180s TTL). dev / `SAMARKET_LOG_HOME_SYNC_FULL_ANALYSIS=1` / `SAMARKET_LOG_HOME_SYNC_BREAKDOWN=1`. 페이로드 필드·HS·unread·realtime·shell·UI 불변 | `lib/community-messenger/home-sync-full-analysis-log.ts`, `lib/community-messenger/home-sync-critical-route-snapshot.ts`, `app/api/community-messenger/home-sync/route.ts`, `docs/messenger-realtime-policy.md`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | prod `full` 은 trace 미전파 시 로그 생략 |
| 2026-05-10 | 메신저 탭 | **silent home-sync 재진입 억제**: tier별 **성공 재생 TTL**(critical 기본 2s·full 4s)·pathname **전환 안정화**·**동일 path 왕복 debounce**; **`[home-sync-suppress]`**·`homeSyncReplaySyntheticReturns` 카운터. HTTP·페이로드·unread·realtime·shell·UI 불변 | `lib/community-messenger/cm-home-silent-lists-fetch.ts`, `lib/community-messenger/cm-home-sync-suppress-log.ts`, `lib/community-messenger/messenger-latency-config.ts`, `lib/runtime/samarket-runtime-debug.ts`, `docs/messenger-realtime-policy.md`, `deployment/messenger-production.env.example`, `docs/trade-perf-hot-path-changelog.md`, `lib/runtime/__tests__/messenger-home-verification-counters.test.ts` | `npx tsc --noEmit`, `vitest messenger-home-verification-counters` | `forceNetwork` 는 재생·suppress 전부 우회 |
| 2026-05-10 | 메신저 탭 | **home-sync trace 계측 정합**: prod critical `token: ""`·`trace?.token` 분기로 bundleSteps·unread ms 가 0으로 남던 문제 → `homeSyncTraceMeterEnabled`·critical **항상 비어 있지 않은 token**·in-process TTL **traceSnapshot**·`[home-sync-trace-gap]`·`[home-sync-route-wall-split]`×3. DB·unread 의미·UI 불변 | `home-sync-trace.ts`, `get-community-messenger-home-sync-bundle.ts`, `service.ts`, `enrich-messenger-trade-unread-with-legacy-trade.ts`, `app/api/community-messenger/home-sync/route.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | gap 은 `|routeBundleAwaitMs−bundleTotalMs|>50` |
| 2026-05-10 | 채팅 진입 | **`GET /api/auth/session`**: `getSession()`+`session.user` 제거 → **`resolveRouteHandlerUserIdFromSupabase`**(`getClaims` 우선·실패 시 `getUser`)로 Supabase **쿠키 user 비신뢰** 콘솔 경고 완화. refresh 경쟁은 `getClaims` 성공 경로에서 회피 | `app/api/auth/session/route.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | `getClaims` 실패 시에만 `getUser` 폴백 |
| 2026-05-10 | 하단 탭 | **BN1 quiet window**: 하단 탭 의도 직후 2.5s 동안 **비목적지 Philife background warm / bottom-nav idle prewarm** 만 지연해 목적지 RSC·hydration·클라 fetch 와 `/api/philife/neighborhood-feed` 경합을 줄임. 목적지 탭 pointerdown `router.prefetch`·클라 cache prewarm 은 유지 | `components/layout/BottomNav.tsx`, `components/community/PhilifeFeedWarmPrefetch.tsx`, `docs/samarket-performance-track-state.md`, `docs/samarket-perf-domain-checksheet.md`, `docs/trade-perf-hot-path-changelog.md` | 수동 nav-perf 3회, `npx tsc --noEmit` | quiet window 는 비목적지 background 작업만; 탭 대상 prewarm 을 늦추면 첫 진입 체감이 악화됨 |
| 2026-05-10 | 하단 탭 | **BN2 stores 목적지 single-flight**: `AbortSignal` 이 있는 `/stores` 클라 fetch 도 `runSingleFlight` 에 합류하게 해 React dev 재마운트·빠른 마운트 경합에서 `/api/stores/home-feed?region=...` 동일 URL 이 중복 서버 요청되는 구조를 제거. abort 는 호출자 promise 만 끊고 네트워크 비행은 공유 | `lib/stores/store-delivery-api-client.ts`, `docs/samarket-performance-track-state.md`, `docs/samarket-perf-domain-checksheet.md`, `docs/trade-perf-hot-path-changelog.md` | 수동 `/market → /stores` nav-perf 3회, `npx tsc --noEmit` | `AbortSignal` 분기에서 single-flight 를 우회하면 목적지 API fan-out 중복 재발 |
| 2026-05-10 | 하단 탭 | **BN3 stores 지역 피드 prewarm 키 정렬**: Stores 탭 pointerdown prewarm 이 기본 피드만 데우던 상태에서 실제 마운트 키(`region`/`district`)도 함께 데워 `/stores` 마운트 후 지역 피드 fetch 가 단일비행/캐시에 합류. 비목적지 quiet window 와 목적지 즉시 prewarm 원칙 유지 | `components/layout/BottomNav.tsx`, `lib/main-menu/bottom-nav-tap-prewarm-data.ts`, `docs/samarket-performance-track-state.md`, `docs/samarket-perf-domain-checksheet.md`, `docs/trade-perf-hot-path-changelog.md` | 수동 pointer `/market → /stores` nav-perf 3회, `npx tsc --noEmit` | 지역 suffix 계산을 `StoreNearbyFeedSection` 과 다르게 바꾸면 prewarm 키 미스 재발 |
| 2026-05-10 | 스토어 API | **BN4 taxonomy 병렬 조회**: `GET /api/stores/taxonomy` 가 categories await 후 topics 를 await 하던 직렬 왕복을 `Promise.all` 로 병렬화해 벽시계 RTT 를 줄임. JSON·에러·topics 테이블 missing 폴백 의미 동일 | `app/api/stores/taxonomy/route.ts`, `docs/samarket-performance-track-state.md`, `docs/samarket-perf-domain-checksheet.md`, `docs/trade-perf-hot-path-changelog.md` | 수동 pointer `/market → /stores` nav-perf 3회, `npx tsc --noEmit` | 직렬로 되돌리면 `/stores` taxonomy slowest 구간 재증가 |
| 2026-05-10 | 하단 탭 | **BN5 taxonomy 클라 TTL + Stores prewarm**: `fetchStoresTaxonomyDeduped` 에 120s 메모리 캐시(HTTP **200** 만)·`isStoresTaxonomyClientCacheFresh` 로 prewarm 중복 방지·`prewarmBottomNavTapTargetClientCache(/stores)` 에 taxonomy 합류. 어드민 taxonomy `reloadTaxonomy` 성공 시 `clearStoresTaxonomyClientCache` | `lib/stores/store-delivery-api-client.ts`, `lib/main-menu/bottom-nav-tap-prewarm-data.ts`, `components/admin/stores/AdminStoreApplicationSettingsPage.tsx`, 성능 문서 | `npx tsc --noEmit` | TTL 제거 시 재진입마다 taxonomy 네트워크 재증가; 어드민 무효화 누락 시 최대 TTL 동안 공개 데이터 스테일 |
| 2026-05-10 | (운영) | **패리티 실행 순서·게이트**: 당근·배민·카톡 동급 맞추기 권장 순서(거래 → 배달형 → 메신저)·라운드별 `npm run verify:parity-gates`(거래 핫패스 계약·메신저 홈 단위검증·tsc). 체크시트 `[x]`는 증거 후에만 | `docs/samarket-parity-execution-order.md`, `docs/samarket-perf-domain-checksheet.md`, `docs/samarket-performance-track-state.md`, `docs/samarket-native-feel-charter.md`, `AGENTS.md`, `package.json` | `npm run verify:parity-gates` | 라우트 검증은 `npm run verify:routes`·`npm run check` 축 |
| 2026-05-10 | 거래 상세 RSC | **P1 related 스트리밍 분리**: `getItemDetailPageData` 가 첫 블록에서 `getTradeDetailRelatedData` 를 await 하지 않음. `/post/[id]` 가 `Suspense` + `PostDetailRelatedDeferredLoader` 로 동일 `getTradeDetailRelatedData`·`preloadedItem` 스트림. 본문·판매자·구매자 제안만 첫 응답에 포함 | `trade-detail.service.ts`, `post/[id]/page.tsx`, `post-detail-related-deferred.tsx`, `PostDetailPageClient.tsx`, `PostDetailView.tsx`, `trade-post-detail-chat-hot-path.mdc`, `app/api/posts/[postId]/related/route.ts`(주석), 성능 문서 | `npm run verify:trade-hot-path-contract`, `npm run verify:parity-gates` | related 를 다시 첫 `Promise.all` 에 넣으면 첫 바이트가 related max 에 묶임 |
| 2026-05-10 | 거래 상세·채팅 | **P1 스트림 마감(점검·구조 변경 없음)**: `getTradeDetailRelatedData` 외부 `loadTradeDetailRelatedBundle` 직접 호출 없음·`openCreateTradeChat` 은 `replace(compose)` 만(동기 `createOrGetChatRoom` 대기 없음). 작업 범위 종료 선언 | `docs/samarket-performance-track-state.md`, `docs/samarket-perf-domain-checksheet.md`, `docs/samarket-parity-execution-order.md` | `npm run verify:trade-hot-path-contract`, `npm run verify:parity-gates` | related 번들 내부 쿼리·캐시는 **새 라운드**에서만 |
| 2026-05-10 | 하단 탭 | **S1 idle·boot `/stores` prewarm 키 BN3 정렬**(마스터 순서 1): idle programmatic prefetch·부트웜이 `prewarmBottomNavTapTargetClientCache(href)` 만 호출하던 것을 `/stores` 에 한해 `storeHomeFeedSuffixFromPrimaryRegion(primaryRegionRef)` 를 `storeHomeFeedSuffixes` 로 전달 — BN3 Stores pointerdown 과 동일 지역 `home-feed` 클라 캐시 키 | `components/layout/BottomNav.tsx`, 성능 문서 | `npm run verify:parity-gates` | suffix 계산을 `BottomNavTabStores` 와 다르게 바꾸면 다시 미스 |
| 2026-05-10 | 하단 탭 | **S1 후속(셸 prewarm 단일화)**: `bottom-nav-prewarm-href.ts` 로 region `/stores` prewarm 공유. `pickMainBottomNavPrefetchHrefs`·세션 부트웜이 `resolveBottomNavTabProgrammaticPrefetchHref` 로 메신저 `?from=` 을 탭 링크와 동일화. `AppSegmentTabs` 부트웜도 동일 region prewarm | `lib/main-menu/bottom-nav-prewarm-href.ts`, `lib/main-menu/main-bottom-nav-prefetch-pick.ts`, `components/layout/BottomNav.tsx`, `components/app-shell/AppSegmentTabs.tsx`, 성능 문서 | `npm run verify:parity-gates` | `pick`/`boot`/`AppSegmentTabs` 중 하나만 구식 href 를 쓰면 RSC·클라 캐시 불일치 재발 |
| 2026-05-13 | 메신저 API | **`POST trade-chat-list-meta` enrich 내부 분해**: `token: trade-chat-list-meta` 시 `tradeListMetaEnrichBootstrapBreakdown`(phase A–D·seed·seller hydrate·gap·top bottleneck) + `hydrateTradeChatListContextMetaForRoomIds` 가 `perf` 에 `enrich_*` 병합. 응답 shape·캐시 계약·메타 의미·unread 정합 변경 없음(관측만) | `lib/community-messenger/service.ts`, `lib/community-messenger/home-sync-trace.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | breakdown 필드 제거 시 `[dev-api-perf]` enrich TOP1 추적 불가 |
| 2026-05-13 | 메신저 API | **`trade-chat-list-meta` direct keys**: `tradeDirectKeysListMetaBreakdown`(`direct_keys_*`) 계측 + 합성 trace **`tier: critical`** 로 HS3 mega bundle·HS2 posts 단일 select 재사용(응답 shape 동일). PATCH·realtime·unread 미변경 | `lib/community-messenger/service.ts`, `lib/community-messenger/home-sync-trace.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | `posts` 가 `TRADE_CHAT_LIST_POST_SELECT_CRITICAL` 미충족 시 빈 썸네일( critical 홈과 동일 알람 경로) |
| 2026-05-13 | 메신저 읽음 | **`PATCH .../rooms/[roomId]` `mark_read`**: 참가자 행 선조회 후 동일 커서·unread 0 이면 RPC 생략·broadcast 생략(`duplicateAckSkipped`). 커서 역행 요청은 DB 유지·broadcast 생략(`regressionBlocked`). 동시 동일 키 요청은 **in-flight 단일화**(broadcast 1회). JSON 응답 필드는 기존과 동일(ok·error·lastReadAt·lastReadMessageId). `[dev-api-perf]` `patch_room_*` | `service.ts`(markCommunityMessengerRoomAsRead), `rooms/[roomId]/route.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | 동시 요청 실패 공유는 극히 드묾 — 재시도 정책은 클라 유지 |
| 2026-05-13 | 메신저 읽음 | **`mark_read` 실중복 가드**: 클라이언트가 항상 `flushOpen` 이라 서버 `rpcMode` 가 open 만 되어 조기 스킵이 막히던 문제 → **정규화된 커서 일치·unread 0 이면 open 포함 조기 스킵**. read_ack broadcast **TTL+inflight dedup (prod)**·`cm-rt-read-sync` skip 로그. inflight 키에 **lastReadMessageId** 포함(다른 메시지 읽음이 잘못 합쳐지지 않게) | `service.ts`, `read-ack-broadcast-server.ts`, `rooms/[roomId]/route.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | open_tail(lastReadMessageId 없음) 는 기존처럼 tail RPC |
| 2026-05-13 | 메신저 읽음 | **`mark_read` item_trade 브리지 비동기화**: 로그상 `mark_read_registry_sync_ms` 는 실제로 `syncItemTradeReadWithMessengerRoomMark`(CM↔item_trade·`product_chats` 미읽음 정합) 구간이었음. **RPC로 CM participant 읽음은 이미 확정** → 해당 브리지를 `after()` 로 응답 이후 실행·동일 커서 3s TTL dedupe. 요청 경로 `mark_read_registry_sync_ms`=0·`registry_sync_background_scheduled`·분해는 배경 `[cm-mark-read-trade-sync]` 로그 | `schedule-item-trade-read-sync-after-messenger-mark.ts`, `sync-item-trade-read-with-messenger-room.ts`, `service.ts`, `rooms/[roomId]/route.ts`, `docs/trade-perf-hot-path-changelog.md` | `npm run verify:trade-hot-path-contract`, `npx tsc --noEmit` | 브리지를 다시 PATCH 직선 await 로 묶으면 RTT 재증가 — 거래 탭 뱃지는 수백 ms eventual |
| 2026-05-13 | 메신저 읽음 (bg) | **item_trade 브리지 내부**: `chat_rooms`·`community_messenger_rooms.summary` 병렬 시드, resolve 내 CM·tail `created_at` 병렬·`unread_recalc_*`/`meta_fallback_*`/`query_count`/TOP 병목 로그. 동일 viewer·CM 방 background **in-flight coalesce**(pending 마지막)·TTL 동일 커서 유지 | `sync-item-trade-read-with-messenger-room.ts`, `schedule-item-trade-read-sync-after-messenger-mark.ts`, `service.ts`, `docs/trade-perf-hot-path-changelog.md` | `npm run verify:trade-hot-path-contract`, `npx tsc --noEmit` | 원장·PC 0 의미 변경 금지 |
| 2026-05-13 | 메신저 읽음 (bg) | **브리지 배경 RTT**: CM 메시지·`chat_messages` 커서 `created_at` **프로세스 내 TTL 캐시**(10s)·`lastReadId` `created_at` 캐시 재사용으로 resolve·read_at 구간 DB 왕복 감소. **동일 user·CM·cursor 5s 내 재실행 전체 스킵**(`registry_background_dedupe_hit`)·스케줄 동일 커서 TTL **3s→5s**. 선택 `communityMessengerLastReadMessageCreatedAt` 로 CM `created_at` 조회 생략 가능(스케줄 인자 확장, 기본 미전달) | `sync-item-trade-read-with-messenger-room.ts`, `schedule-item-trade-read-sync-after-messenger-mark.ts`, `docs/trade-perf-hot-path-changelog.md` | `npm run verify:trade-hot-path-contract`, `npx tsc --noEmit` | 캐시 TTL 내 메시지 시각 변경은 이론상 스테일 가능(짧은 TTL)·`lastReadMessageId` 후퇴·RPC 의미 변경 없음 |
| 2026-05-13 | 메신저 읽음 (bg) | **브리지 내부 no-op·계측 정정**: `unread_recalc_count_query_ms` 가 실제 COUNT가 아니라 participant+read_at 합으로 오기재되던 것을 **0 + `unread_count_query_mode:none`** 로 분리. **participant 선조회·동일 커서·unread 0** 이면 participant UPDATE 생략·`read_at` 은 probe 후 없으면 UPDATE 생략. **`community_messenger_rooms.summary` 10s TTL 캐시**. meta `product_chats` 는 **동일 select로 unread 측정 후 이미 0이면 UPDATE 생략**. PATCH·응답·realtime·원장 의미 변경 없음 | `sync-item-trade-read-with-messenger-room.ts`, `docs/trade-perf-hot-path-changelog.md` | `npm run verify:trade-hot-path-contract`, `npx tsc --noEmit` | no-op 조건을 완화하면 미읽음·read_at 정합이 깨질 수 있음 |
| 2026-05-13 | 메신저 홈 | **home-sync HS5 unread**: 레거시 `chat_rooms`/`product_chats` 번들 페치에 **프로세스 내 2.5s TTL 행 캐시 + 동일 fingerprint in-flight 단일화**(participant unread·room·PC id 시그니처 포함). **`[home-sync-unread-bootstrap]`** snake_case 분해 로그·`unreadHomeSyncSteps` 필드 확장. 응답 shape·silent_delta·room ordering·RPC 의미 불변 | `enrich-messenger-trade-unread-with-legacy-trade.ts`, `home-sync-trace.ts`, `service.ts`, `app/api/community-messenger/home-sync/route.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | TTL 내 레거시 카운트만 바뀌는 극히 드문 스테일 가능(2.5s)·정합 이슈 시 TTL 축소 |
| 2026-05-13 | 메신저 API | **`POST trade-chat-list-meta` seller/profiles**: `fetchProfilesByIds` 를 **프로필 id 단위 짧은 TTL(기본 5s, `SAMARKET_PROFILE_ROW_CACHE_TTL_MS`)** + **동일 missing 집합 single-flight** 로 seller 라벨 조회 RTT·중복 왕복 감소. `token: trade-chat-list-meta` 시 `tradeListMetaProfileHydrateStats` 를 `perf` 에 병합. 기존 5분 batch-key 캐시 제거. `{ ok, patches }`·`contextMeta` 필드 의미 동일 | `lib/community-messenger/service.ts`, `lib/community-messenger/home-sync-trace.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | 짧은 TTL 구간 프로필 표시명만 스테일 가능·`query_count` 근사식은 enrich 경로와 동일 |
| 2026-05-13 | 메신저 홈·목록 | **direct_keys Phase1 bridge**: `enrichTradeRoomContextMetaFromDirectKeys` legacy 경로의 `product_chats`(in id)·`home_sync_direct_keys_item_trade_rows`·`chat_rooms` item_trade 폴백에 **프로세스 내 5~30s 스냅샷(기본 20s, `SAMARKET_DIRECT_KEYS_BRIDGE_CACHE_TTL_MS`) + single-flight**. `tradeDirectKeysListMetaBreakdown` 에 category/bridge 슬롯·캐시·singleflight 필드 확장·`home-sync-*` 트레이스에도 동일 breakdown 부착. `TradeCategoryMetaRequestLoader.lastEnsureCategoryUsedDb` 로 category RTT 여부 표시. 응답 shape·bridge/category 의미·정합 동일 | `lib/community-messenger/service.ts`, `lib/community-messenger/home-sync-trace.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | TTL 내 원장·PC 매핑만 스테일 가능 — 짧은 TTL 유지 |
| 2026-05-13 | 메신저 dev 메모리 | **monitoring `server-store` 그래프 분리**: `server-store-record`(이벤트·부트스트랩 분해·HS4 진단 **동적 import**) vs `server-store-summary`(SLO·요약 JSON). `monitoring/events`·`service`·부트스트랩 등은 record만 import. 요약 라우트만 summary. dev 이벤트 창 기본 **240**(`SAMARKET_MONITORING_MAX_EVENTS_DEV`로 조정). `server-store-root` 에 `getMessengerMonitoringStoreFootprint`. `instrumentation-dev-memory-watch` 에 SIGTERM 로그·스토어 footprint. 런타임 API 응답 shape·이벤트 유실 없음 | `lib/community-messenger/monitoring/server-store*.ts`, `lib/dev/instrumentation-dev-memory-watch.ts`, `app/api/.../monitoring/*`, `service.ts` 등 | `npx tsc --noEmit` | HS4 진단 로그만 비동기 지연 가능(기능 동일) |
| 2026-05-13 | trade-chat-list-meta enrich | **`token: trade-chat-list-meta` 오케스트레이션**: Phase B/C를 `summaries` 전역 스캔 대신 `pcByMessengerRoomId`·`crByRoomId` 맵 순회로 접음(동일 스킵·동일 `buildTradeMessengerListContextMetaFromLoadedPost`). Phase A·B·C·D `contextMeta` 할당 카운트, B→C CPU 병합 직전까지 벽시계 간격(`orchestration_phase_transition_wait_ms`), 맵 재구성 수, `orchestration_*` 진단 필드를 `tradeListMetaEnrichBootstrapBreakdown`에 병합. 응답 shape·room ordering·unread·direct_keys 의미·realtime 불변 | `lib/community-messenger/service.ts`, `lib/community-messenger/home-sync-trace.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | 맵 키 집합을 바꾸면 썸네일 hydrate 누락 가능 — ledger/PC 맵과 동일 소스 유지 |
| 2026-05-13 | 메신저 dev monitoring | **in-process monitoring store 메모리·CPU**: `subscriptionEventLog`로 `channel_subscribe`(outcome)만 분리 보관·롤업·outcome 재계산 시 `events` 전역 스캔 제거. `call.signaling`은 증분 `bumpOutcome`만. dev 기본 `MAX_EVENTS` 240→160. 이벤트/알림/구독로그 trim·EMA 페이로드 크기·추정 heap·`monitoring_store_*` 필드를 `getMessengerMonitoringStoreFootprint`·`[dev-memory-watch]`에 노출. 요약 API shape·알림 규칙·이벤트 의미(집계·SLO) 불변 | `lib/community-messenger/monitoring/server-store-root.ts`, `server-store-record.ts`, `lib/dev/instrumentation-dev-memory-watch.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | 구독 로그 백필 전까지 짧은 구간 outcome 스테일 가능(첫 요청 직후)·`SAMARKET_MONITORING_MAX_EVENTS_DEV`로 상한 조정 |
| 2026-05-13 | GET /api/me/profile · direct_keys | **ensure provider no-op**: `profiles.provider`·`provider_user_id`·`auth_provider` 가 이미 auth identity 와 일치하면 `persistProviderIdentityIfMissing` PATCH 생략(`ensure_profile_result: skipped`). PATCH 시 `ensure_profile_patch_*`·persist reason·`[dev-api-perf]` 확장. **direct_keys**: mega/legacy 맵 병합 시 중복 키 카운트·`roomToParsed`+`targetByRoom` 단일 순회 apply·`direct_keys_*` orchestration 필드(`tradeDirectKeysDetail`·`tradeDirectKeysListMetaBreakdown`). 응답 JSON `ok/profile`·direct_key 의미·unread·realtime 불변 | `lib/auth/ensure-user-profile.ts`, `app/api/me/profile/route.ts`, `lib/community-messenger/service.ts`, `lib/community-messenger/home-sync-trace.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | RPC 중복 행이 동일 키로 덮어쓰면 기존과 동일(마지막 승)·hidden_ms 는 벽시계 잔차 근사 |
| 2026-05-13 | trade-chat-list-meta enrich | **`TradeCategoryMetaRequestLoader`**: `categories`/`trade_categories` `in(id)` 를 **정렬 id 키 + 프로세스 단일 inflight** 로 동시 요청·병렬 `Promise.all` 중복 RTT 합류. 리더 merge 직후 **모듈 TTL 스냅샷 즉시 기록**(`tradeCategoryWriteModuleSnapshotFromMerged`)·`categoryFetchDetail` 에 `category_*` 진단·`tradeMetaSingleflightHit` 연동. **orchestration**: B/C **실제 contextMeta 할당 시에만** phase iteration 증가·`orchestration_room_loop_count_after`·`attach_pass`·`summary_scan_after`·`parallel_wait_after`·`lookup_reuse_hit` 등. 응답 shape·카테고리 라벨 의미·room ordering·unread·realtime 불변 | `lib/community-messenger/service.ts`, `lib/community-messenger/home-sync-trace.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | inflight 키를 id 정렬 없이 쓰면 합류 실패·동일 5분 TTL 내 테이블 변경은 기존과 동일 스테일 창 |
| 2026-05-13 | trade-chat-list-meta · home-sync trade meta | **카테고리 20s 배치 스냅샷**(`tradeCategoryBatchRowSnapshotCache`): 동일 정렬 `in(id)` 집합에 대해 **DB 직전** 배치 행 재적용·miss 카운터 보정. **fetchTable** 성공 시 배치 스냅샷 저장. **Phase C** 는 Phase B 가 이미 메타를 붙인 rid 에 대해 **`touchTradeRoomMeta` 생략**으로 duplicate_apply 감소(contextMeta 는 기존과 같이 갱신). `orchestration_attach_pass_count_after`·mega `direct_keys_category_batch_singleflight_joins` 로드 peek. 응답 shape·카테고리 의미·room ordering·unread·realtime 불변 | `lib/community-messenger/service.ts`, `lib/community-messenger/home-sync-trace.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | 배치 TTL(20s) 내 카테고리 표기만 짧은 스테일 가능·B 미경로 방은 C touch 기존과 동일 |
| 2026-05-13 | trade-chat-list-meta enrich | **trade_meta 병렬·Phase C**: Phase A+bridge 와 동시에 **요약·시드 post id**로 `categoryLoader.ensureForPosts` 선행(`categoryParallelPrimePromise`, 계측 `enrich_category_prime_parallel_ms`·`enrich_parallel_*`). Phase C 에서 **`fetchPostsCached` 와 `product_chats` in(post_id) 후보 쿼리를 `Promise.all`로 겹침**(의미·행 집합 동일). `categoryFetchDetail` 에 `category_duplicate_attach_count`·`category_singleflight_hit`·`category_lookup_reuse_hit`·`category_cache_hit_after`·append 병합. `orchestration_lookup_reuse_hit` 에 category lookup reuse 반영. **direct_keys** `direct_keys_category_attach_iterations` 는 **유니크 category id 수**로 정렬(슬롯 중복과 분리). 응답 shape·카테고리 매핑·room ordering·unread·realtime 불변 | `lib/community-messenger/service.ts`, `lib/community-messenger/home-sync-trace.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | prime/병렬 순서 변경 시 동일 요청 내 ensure 호출 순서만 달라질 수 있으나 동일 post→category 병합 규칙 유지 |
| 2026-05-13 | trade-chat-list-meta · compile | **rooms+participants ∥ room_profiles**: `fetchRoomsPayloadByRoomIds` 가 Supabase 경로에서 `community_messenger_rooms`·`participants` 와 `room_profiles` in(room_id) 를 **동일 벽시계로 병렬**(`roomsProfilesParallelWallMs`, `trade_chat_meta_rooms_profiles_parallel_wall_ms`·`…saved_ms_approx`). **라우트**: `hydrateTradeChatListContextMetaForRoomIds` 를 `service` **동적 import**로 분리해 번들 정적 그래프 재진입 완화. `trade_meta_runtime_top_after` = `trade_chat_meta_top_bottleneck` 동일 키 복제(진단). 응답 `patches`·participant unread·프로필 행 의미 동일 | `lib/community-messenger/service.ts`, `app/api/community-messenger/trade-chat-list-meta/route.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | room_profiles 는 요청 room id 전체 기준 조회(기존은 roomRows 부분집합)·미존재 방 id 는 빈 결과만 추가 |
| 2026-05-13 | trade-chat-list-meta · PATCH mark_read · home-sync compile | **category `lastEnsureCategoryUsedDb` 정합**: 이전에는 miss 가 있으면 **singleflight 조인만** 해도 `lastEnsureCategoryUsedDb=true` 로 남아 `category_cache_hit_after`·`direct_keys_category_cache_hit_after` 가 과소 적중. **이제 PostgREST `in(id)` 리더 요청이 실제로 나간 경우만** true. **category 계측**: `category_query_wall_ms`·`category_postgrest_wait_ms`·`category_network_wait_ms`(현재 query_wall 동일)·`category_attach_cpu_ms`(merge+batch snap)·`category_serialize_ms`(0)·`category_request_local_*`·`category_process_cache_hit`·`category_cache_store_reason`. **mark_read 스냅샷**: `mark-read-participant-snapshot.ts` 로 분리·**커서가 있을 때만 TTL SELECT 생략**(flush-only는 정합상 생략 안 함)·성공 후 **다중 키 저장**·`mark_read_snapshot_cache_key`/`snapshot_*` diag. **compile**: critical `recordMessengerMonitoringEvent` 를 `server-store-record` **동적 import**로 분리 | `lib/community-messenger/service.ts`, `lib/community-messenger/mark-read-participant-snapshot.ts`, `lib/community-messenger/home-sync-trace.ts`, `app/api/community-messenger/rooms/[roomId]/route.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | flushOpen+messageId 없음 PATCH는 TTL fast path 없음(의도)·모니터링 이벤트는 fire-and-forget 지연 가능 |
| 2026-05-13 | trade-chat-list-meta enrich · direct_keys | **2차 안정화**: `token: trade-chat-list-meta` 이고 요약 `postId` 가 있으면 **`fetchSeedProductChatsForTradeEnrich` 와 요약 전용 category prime 을 `Promise.all` 로 겹침**·이후 `categoryParallelPrimePromise` 는 시드 post 중 요약에 없는 id 만 ensure(중복 ensure 제거). **`categoryPrimeParallelWallMs`** 는 요약 prime 벽시계를 **누적**(`+=`). **`lmSeedMs`** 는 시드 분기 공통으로 `seedMsRef`/wall fallback. **mega bundle LRU**: `directKeysMegaBundleCache` prune 상한 **256→192** 로 dev 프로세스 RSS 압력 완화(키·TTL·응답 shape 동일). unread·realtime·room ordering·JSON 필드 불변 | `lib/community-messenger/service.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit`, `npm run verify:trade-hot-path-contract` | prime 집합을 넓히면 동일 요청 내 ensure 순서만 달라질 수 있음(병합 규칙 동일)·mega 상한 축소는 캐시 적중률만 영향 |
| 2026-05-13 | 메신저 API compile 2차 | **home-sync 라우트 compile 그래프 분리**: 방 상한 상수 **`home-sync-room-caps.ts`**. **`getCommunityMessengerHomeSyncBundle`**: 라우트는 payload **`import type`** 만·실제 호출은 **캐시 miss·in-flight 리더일 때만** `await import(get-bundle)`(singleflight 합류는 import 생략). **`presence`·`incoming`**: `service` 동적 import. **`monitoring/events`**: `server-store-record` 동적 import·`NextResponse` 제거. `service` 는 caps re-export. `get-bundle` 은 RPC만 `service` 정적 import. 응답·상한·의미 동일 | `home-sync-room-caps.ts`, `service.ts`, `home-sync/route.ts`, `get-community-messenger-home-sync-bundle.ts`, `presence/route.ts`, `calls/sessions/incoming/route.ts`, `monitoring/events/route.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit`, `npm run verify:trade-hot-path-contract` | 상한 변경 시 caps·`listCommunityMessengerMyChatsAndGroups` 동시 수정 |
| 2026-05-13 | 메신저 API compile 3차 | **trade-post-preview·thumbnail·bootstrap**: `trade-post-list-preview` / `trade-post-thumbnail` 가 `getSupabaseServer`·헤드라인·가격·썸네일 URL 유틸을 **`Promise.all` 동적 import**로 분리(응답 JSON·posts SELECT 계약 동일). **`GET bootstrap`**: `service`·`messenger-api-route-timing`·`server-store-record` 를 **인증·rate limit 직후 `Promise.all` 동적 import**·캐시 타입은 `CommunityMessengerBootstrap`(`types`) + `CommunityMessengerBootstrapDiagnostics`(`import type` from `service`). 런타임 부트스트랩 페이로드·진단 의미 동일 | `app/api/community-messenger/trade-post-list-preview/route.ts`, `trade-post-thumbnail/route.ts`, `bootstrap/route.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit`, `npm run verify:trade-hot-path-contract` | 첫 요청에 모듈 로드 1회 추가(이후 캐시)·IDE는 `import type service` 로 여전히 타입 의존 |
| 2026-05-13 | 메신저 API compile 4차 | **messages·room PATCH compile 클러스터**: `rooms/[roomId]/messages` 가 `messenger-api-route-timing`·`publish-messenger-room-bump` 를 **동적 import**(GET/POST rate limit 이후·bump는 전송 성공 시만). **`rooms/[roomId]` GET/PATCH**: `service` **캐논 확정 후 동적 import**·`CommunityMessengerMarkReadResult`/`MarkReadDiag` 는 **`import type`**. **`mark_read` read-ack**: `read-ack-broadcast-server` 는 **broadcast 필요 시만** 동적 import. PATCH·mark_read JSON·unread·broadcast 의미 동일 | `app/api/community-messenger/rooms/[roomId]/messages/route.ts`, `rooms/[roomId]/route.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit`, `npm run verify:trade-hot-path-contract` | 첫 히트에 timing/store 모듈 로드 1회·타입은 여전히 `service` 참조 |
| 2026-05-13 | home-sync full · direct_keys | **mega bundle 을 full home-sync 에 확장**: 기존 mega RPC(`home_sync_direct_keys_critical_bundle`)는 **`trace.tier === "critical"` 만** 탔고 full 은 legacy multi-RTT → `trade_meta_cache_hit` 가 category miss·posts detail 과 결합해 false 로 남기 쉬움. 이제 **`tier === "full"`** 또는 **`roomListCap` 양수(home-sync 전용, prod full trace 없음 포함)** 이면 동일 mega 경로 허용·`TradeEnrichBootstrapSharedCtx.homeSyncMegaBundleForDirectKeys` 전달. **`fetchTradeChatListPostRowsByIds`**: 스키마 cold pack 성공(`usedSel` 있음) 시 **`cacheHit: true`** 로 계측(스키마 확정 후 단일 select 의미). `phaseDependencyReason` 에 `full_mega_bundle_rpc`/`home_sync_mega_bundle_rpc` 분기. 응답 contextMeta·room ordering·unread·realtime·mega TTL 동일 | `lib/community-messenger/service.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit`, `npm run verify:trade-hot-path-contract` | RPC 페이로드 한도 초과 시 기존처럼 integrity 실패 → legacy 폴백 |
| 2026-05-13 | dev 메모리 · compile 5차 · mega 계측 | **CM 서비스 캐시 footprint**: `getCommunityMessengerServiceCacheFootprint`(mega/bridge/category/room snapshot inflight 등 **엔트리 수만**). **`instrumentation-dev-memory-watch`**: `cm_service_cache_footprint`·`memory_cm_cache_total_entries`·`memory_module_cache_suspicion_flag`(rss≥3500 & 캐시 합≥80). **mega LRU** 192→128·**category batch 스냅샷** 상한 512→384. **mega 무결성 실패** 시 `tradeDirectKeysDetail.phaseDependencyReason=mega_bundle_incomplete_legacy_fallback` + `mega_bundle_integrity_*`·성공 경로에 `mega_bundle_integrity_ok: true`. **`GET /api/chat/room/.../bootstrap`**: trade bootstrap 체인 **`Promise.all` 동적 import**. **CM rooms/messages**: `messenger-room-canonical-resolve-api`·`context_meta` 의 `room-context-meta` 를 요청 시 동적 import. 응답·의미·unread·realtime 동일 | `lib/community-messenger/service.ts`, `lib/community-messenger/home-sync-trace.ts`, `lib/dev/instrumentation-dev-memory-watch.ts`, `app/api/chat/room/[roomId]/bootstrap/route.ts`, `app/api/community-messenger/rooms/[roomId]/route.ts`, `.../messages/route.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit`, `npm run verify:trade-hot-path-contract` | 캐시 상한 축소는 적중률·짧은 스테일 창만 영향 |
| 2026-05-13 | instrumentation 번들 | **Webpack `Can't resolve 'crypto'` 수정**: `instrumentation` → `instrumentation-dev-memory-watch` 가 **`service` 동적 import**를 분석하면서 `service.ts` 의 Node `crypto` 가 클라이언트/Edge 그래프에 끌림. **해결**: `cm-service-cache-footprint-registry`(Node·crypto 무의존)에 getter 등록·`service` 로드 시 `registerCommunityMessengerServiceCacheFootprintGetter`·memory-watch 는 registry 만 호출. footprint·의미 동일·첫 CM API 전까지 `cm_service_cache_footprint` 는 null 가능 | `lib/community-messenger/dev/cm-service-cache-footprint-registry.ts`, `lib/community-messenger/service.ts`, `lib/dev/instrumentation-dev-memory-watch.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit` | registry 없이 `service` 를 instrumentation 경로에서 다시 import하지 말 것 |
| 2026-05-13 | home-sync mega bridge | **mega RPC `rpc_cold` 완화**: mega 스냅샷 TTL 을 bridge 레거시(20s)와 분리·**기본 28s**(`SAMARKET_DIRECT_KEYS_MEGA_BUNDLE_CACHE_TTL_MS`, 12~60s). `directKeysMegaBundleCache` prune **128→192**. integrity 통과 후에만 기록·응답·RPC 의미 동일 | `lib/community-messenger/service.ts`, `docs/trade-perf-hot-path-changelog.md` | `npx tsc --noEmit`, `npm run verify:trade-hot-path-contract` | TTL 상한을 과하게 올리면 목록 trade 메타만 길게 스테일 가능 |
| 2026-05-19 | 오너 허브 배지 unread | **cold unread parts RPC**: `hub_badge_user_chat_unread_parts` — participants+rooms+product_chats+item_trade hint 단일 SQL. TS는 RPC 우선·legacy multi-query 폴백·`[unread-parts-query]` 계측. `route-perf` `unread_parts_ms`·`unread_parts_via`. 응답 JSON·4s unread TTL·hub TTL warm 구조 불변 | `supabase/migrations/20260519120000_hub_badge_user_chat_unread_parts_rpc.sql`, `lib/chat/user-chat-unread-parts.ts`, `app/api/me/store-owner-hub-badge/route.ts`, `scripts/apply-hub-badge-unread-rpc.mjs` | `node scripts/apply-hub-badge-unread-rpc.mjs`, `node scripts/measure-owner-hub-badge-perf.mjs`, `tsc` | RPC 미적용 시 legacy ~1.2s 유지 |
| 2026-05-19 | 오너 허브 배지 store_attention memory | **process memory TTL(5s)**: `hub-store-attention-memory-cache` — key `hub_store_id`, memory→RPC→legacy 3-count. prefetch 유지·wave3 memory hit 시 prefetch await 생략. `store_attention_via=memory`·`store_attention_memory_*` | `hub-store-attention-memory-cache.ts`, `get-owner-hub-store-attention-counts.ts`, `build-owner-hub-badge-payload.ts` | `measure-owner-hub-badge-perf.mjs`, `tsc` | wave1·wave2 memory 캐시 불변 |
| 2026-05-19 | 오너 허브 배지 store_order_unread memory | **process memory TTL(5s)**: `hub-store-order-unread-memory-cache` — key `user_id\|hub_store_id`, memory→store_orders+participants query. `store_order_unread_via` memory\|query\|empty_orders. `invalidateHubStoreOrderUnreadMemory` — hub badge invalidate 연동 | `hub-store-order-unread-memory-cache.ts`, `store-order-chat-service.ts` | `measure-owner-hub-badge-perf.mjs`, `tsc` | wave1·cm_unread·find_hub 불변 |
| 2026-05-19 | 오너 허브 배지 cm_unread memory | **process memory TTL(5s)**: `cm-unread-room-count-memory-cache` — memory→`get_community_messenger_unread_room_count` RPC→PostgREST count head. `cm_unread_via=memory`·`cm_unread_memory_*` 계측. `invalidateCommunityMessengerUnreadTotalCache` — hub badge invalidate 연동 | `cm-unread-room-count-memory-cache.ts`, `community-messenger-unread-total.ts`, `hub-badge-wave2-perf.ts` | `measure-owner-hub-badge-perf.mjs`, `tsc` | unread_parts·find_hub_store 불변 |
| 2026-05-19 | 오너 허브 배지 unread_parts memory | **process memory TTL(5s)**: `hub-badge-unread-parts-memory-cache` — memory→counter→RPC/legacy. `unread_parts_via=memory`·`unread_memory_*` 계측. counter/RPC fallback·의미·JSON 불변 | `hub-badge-unread-parts-memory-cache.ts`, `user-chat-unread-parts.ts`, `hub-badge-breakdown.ts` | `measure-owner-hub-badge-perf.mjs`, `tsc` | invalidate 시 memory+counter bypass 유지 |
| 2026-05-19 | 오너 허브 배지 find_hub_store | **process memory TTL(45s)**: `owner-hub-store-lookup-cache` — `findOwnerHubStore` memory hit 시 PostgREST 생략·miss 시 기존 embed fallback. `find_hub_store_via` memory\|postgrest·`find_hub_store_cache_*` 계측. `invalidateOwnerHubStoreLookupCache` — hub badge invalidate 연동. 응답 JSON·unread counter·cm_unread·store_attention 불변 | `lib/chats/owner-hub-store-lookup-cache.ts`, `build-owner-hub-badge-payload.ts`, `find-owner-hub-store-perf.ts`, `owner-hub-badge-cache.ts` | `measure-owner-hub-badge-perf.mjs`, `tsc` | TTL 내 매장·권한 변경은 지연 가능 — 관리자 변경 후 invalidate 호출 권장 |
| 2026-05-19 | 오너 허브 배지 unread counter | **wave1 read-through**: `hub_badge_user_unread_counters` — RPC 4필드 스냅샷·TTL 5s·miss 시 `hub_badge_user_chat_unread_parts` upsert. `unread_parts_via` counter\|rpc\|legacy·audit `HUB_BADGE_UNREAD_COUNTER_AUDIT`. 응답 JSON·cm_unread·find_hub_store·store_attention 불변 | `supabase/migrations/20260522120000_hub_badge_user_unread_counters.sql`, `lib/chat/hub-badge-unread-counter.ts`, `lib/chat/user-chat-unread-parts.ts`, `hub-badge-breakdown.ts` | `apply-hub-badge-unread-counter-table`, `measure-owner-hub-badge-perf.mjs`, `tsc` | 테이블 없으면 RPC 경로·invalidate 시 5s bypass |
| 2026-05-19 | 오너 허브 배지 cm_unread | **wave2 CM unread RPC**: `get_community_messenger_unread_room_count(p_user_id)` + `idx_cmp_user_unread_positive`. TS RPC 우선·PostgREST count head legacy·`cm_unread_via`·`cm_unread_rpc_ms`·`cm_unread_legacy_ms`. 응답 JSON·wave1 unread·store_order_unread·cache/defer 불변 | `supabase/migrations/20260521120000_get_community_messenger_unread_room_count_rpc.sql`, `lib/community-messenger/community-messenger-unread-total.ts`, `lib/chats/hub-badge-wave2-perf.ts`, `hub-badge-breakdown.ts`, `scripts/apply-hub-badge-cm-unread-rpc.mjs` | `node scripts/apply-hub-badge-cm-unread-rpc.mjs`, `node scripts/measure-owner-hub-badge-perf.mjs`, `tsc` | RPC 미적용 시 legacy ~222ms cm_unread 유지 |
| 2026-05-19 | 오너 허브 배지 store_attention | **wave3 단일 RPC** + **wave1 직후 prefetch**(wave2와 RTT 겹침, `store_attention_rpc_ms`는 prefetch 시작~완료): `get_owner_hub_store_attention_counts(p_store_id)` — refund_requested·pending 주문·open 문의 count를 1 RTT. TS RPC 우선·legacy 3× count 폴백. `[hub-badge-breakdown]` `store_attention_via`·`store_attention_rpc_ms`·`store_attention_legacy_ms`. 응답 JSON·unread·find_hub_store·cache/defer 불변 | `supabase/migrations/20260520120000_get_owner_hub_store_attention_counts_rpc.sql`, `lib/stores/get-owner-hub-store-attention-counts.ts`, `lib/chats/build-owner-hub-badge-payload.ts`, `lib/chats/hub-badge-breakdown.ts`, `scripts/measure-owner-hub-badge-perf.mjs` | `npx supabase db push`, `node scripts/measure-owner-hub-badge-perf.mjs`, `tsc` | RPC 미적용 시 legacy ~227ms wave3 유지 |
| 2026-05-19 | 오너 허브 배지 | **first paint blocking 제거**: `fetchOwnerHubBadgeNow` 는 App Boot·`enableOwnerHubBadgeBackgroundHydration` 이후 rAF×2·idle·visible 에서만 네트워크. 경로·visibility·이벤트는 `scheduleDeferred`·5s plain 중복 방지·라우트 전환 시 `cancelPending`. **쿼리**: `ownerHasAnyStore`+`findOwnerHubStore` 이중 조회 → `findOwnerHubStore` 1회. **캐시 계측**: `hub_badge_route_cache_key`·`exists_query_used` TTL hit 시 0. 응답 JSON·UI·RLS 불변 | `lib/chats/owner-hub-badge-store.ts`, `build-owner-hub-badge-payload.ts`, `owner-hub-badge-cache.ts`, `app/api/me/store-owner-hub-badge/route.ts`, `OwnerHubBadgeRuntime.tsx`, `route-perf-dedupe-fields.ts` | `npx tsc --noEmit`, 수동 `[route-perf]` 3회 warm | 헤더 없는 비-store 호출은 여전히 `deferred:false` 로 남음(관측용)·store 경로만 deferred:true 보장 |
| 2026-05-19 | 오너 허브 배지 **최종 검증·회귀 방지** | **5단 process memory + route TTL + DB counter** — cold run2 stage `*_via=memory`(≤20ms), run3 `cache_hit=1`·`db_ms≈0`. 측정: `node scripts/measure-owner-hub-badge-perf.mjs`(dev 재시작 후). **금지**: TTL·응답 JSON·defer 헤더 계약 역행 | 아래 「Hub badge 회귀 방지」절 | `measure-owner-hub-badge-perf.mjs` 3-run | hub badge 단일 RTT 병목 종료 → 앱 전체 `[route-perf]` 재선정 |
| 2026-06-08 | 오너 허브 배지 cold path | **counter row + notification_targets bundle 병렬화**(phase 1) 및 **counter row nt_* bundle embed**(phase 2 migration). read: `Promise.all` wall ~230ms; `nt_bundle_at` hit 시 bundle RPC skip(1 RTT). stale SWR·route TTL·badge 의미·알림 write path 불변 | `lib/chats/owner-hub-badge-snapshot.ts`, `lib/chats/hub-badge-deep-breakdown.ts`, `supabase/migrations/20260608120000_hub_badge_notification_target_bundle_columns.sql` | `npx tsc --noEmit`, `npm run build`, `measure-owner-hub-badge-perf.mjs` | migration 미적용 시 parallel only; embed 채우기 전 `target_bundle_rpc_skipped=0` |

---

## Hub badge 회귀 방지 (2026-05-19 확정)

### 캐시 계층 (바깥 → 안)

| 계층 | 키 | TTL | invalidate | fallback |
|------|-----|-----|------------|----------|
| Route 응답 | `owner-hub-badge:{userId}` | 5s (`OWNER_HUB_BADGE_TTL_MS`) | `invalidateOwnerHubBadgeCache` | cold rebuild |
| unread_parts memory | `userId` | 5s | `invalidateUserChatUnreadCache` / hub invalidate | counter → RPC → legacy |
| unread DB counter | `user_id` PK | 5s row `updated_at` | bypass 5s on invalidate | RPC → legacy |
| find_hub_store memory | `userId` | 45s | `invalidateOwnerHubStoreLookupCache` | PostgREST embed |
| cm_unread memory | `userId` | 5s | `invalidateCommunityMessengerUnreadTotalCache` | RPC → count head |
| store_order_unread memory | `userId\|hubStoreId` | 5s | `invalidateHubStoreOrderUnreadMemory` | store_orders+parts |
| store_attention memory | `hubStoreId` | 5s | `invalidateHubStoreAttentionMemory` (hub 시 전체 clear) | RPC → 3× legacy count |

### 고정 성능 기준 (측정 스크립트·qqqq dev)

- **헤더**: `x-samarket-hub-badge-deferred:1`, `x-samarket-first-paint-blocking:0` → `[route-perf]` `deferred:true`, `first_paint_blocking:false`
- **run1**: `hubBadgeBypass=1&cmFresh=1&*Fresh=1` — stage fill(RPC/query/postgrest)
- **run2** (4.2s gap): `unread_parts_via`·`find_hub_store_via`·`cm_unread_via`·`store_order_unread_via`·`store_attention_via` = **memory**, stage ms ≤20, `db_ms` ≤50
- **run3** (6s gap): `cache_hit:1`, `db_ms` 0~50
- **로그 태그**: `[hub-badge-unread-parts-memory-hit]`, `[find-hub-store-memory-hit]`, `[cm-unread-memory-hit]`, `[store-order-unread-memory-hit]`, `[store-attention-memory-hit]`

### Before / After (cold path, 대표)

| 지표 | before (단일 RTT 병목) | after (run2 memory) |
|------|------------------------|---------------------|
| unread_parts | ~200–460ms RPC/counter | 0–20ms memory |
| find_hub_store | ~205ms PostgREST | 0ms memory |
| cm_unread | ~204ms RPC | 0–20ms memory |
| store_order_unread | ~198–225ms query | 0–20ms memory |
| store_attention | ~25–250ms RPC | 0–20ms memory |
| db_ms (cold rebuild) | ~420–700 | ≤50 (stage memory 합) |
| run3 route | — | cache_hit:1, db_ms≈0 (plain GET, run2 후 **&lt;5s**) |

### 최종 3-run 확정 (2026-05-19, qqqq, dev 재시작·`caller_component=measure_script` 필터)

측정: `HUB_BADGE_TERMINAL_LOG=<dev 터미널>` · `node scripts/measure-owner-hub-badge-perf.mjs`  
로그 5종 memory-hit 태그 전부 확인됨.

| run | total_ms | db_ms | cache_hit | unread_parts_via | find_hub_store_via | cm_unread_via | store_order_unread_via | store_attention_via | deferred | first_paint_blocking | 판정 |
|-----|----------|-------|-----------|------------------|--------------------|--------------|-------------------------|---------------------|----------|----------------------|------|
| 1 | 694 | 672 | 0 | rpc | postgrest | rpc | empty_orders | rpc | true | false | run1 fill (`*Fresh=1`) |
| 2 | **11** | **3** | 0 | **memory** | **memory** | **memory** | **memory** | **memory** | true | false | **✓ 합격** |
| 3 | 21* | 3* | 0* | memory | memory | memory | memory | memory | true | false | stage warm (*`cmFresh` bypass, route TTL은 plain GET·&lt;5s) |

run2: wave1/2/3 각 0–1ms, `[route-perf]` `badge_query_ms: 3`. run3 route JSON TTL hit 재현: run2 직후 **4.2s 이내** plain `GET /api/me/store-owner-hub-badge` (쿼리 없음) → `cache_hit:1`·`db_ms:0` 기대.

### 회귀 체크리스트 (PR·성능 라운드)

1. `npm run verify:trade-hot-path-contract` — hub badge 건드린 경우 선택
2. `npx tsc --noEmit`
3. dev 재시작 → `node scripts/measure-owner-hub-badge-perf.mjs` — run2 memory·run3 warm
4. `[route-perf]` / `[hub-badge-breakdown]` — `deferred`·`first_paint_blocking`·`*_via` 역행 없음
5. `invalidateOwnerHubBadgeCache` 호출 경로(읽음·CM 송신·주문)에서 hub+stage memory 함께 비움
6. 응답 JSON 필드·합산(`total`, `storesTabAttention` 등) diff 없음
7. **다음 병목**은 hub badge가 아닌 전역 `[route-perf]` (bootstrap, profile, notifications 등)

| 2026-05-21 | owner dashboard waterfall (C) | **원인:** 첫 paint 전 order-counts·orders·settlements 동시/await. **개선:** `[owner-dashboard-waterfall]`·RSC opsSnapshotSeed·paint 후 fetch·orders/settlements 6s background·skeleton first. **미변경:** API SQL·auth·hub | `owner-dashboard-waterfall.ts`, `BusinessAdminDashboard.tsx`, `useOwnerHubSubroutePrefetch.ts`, `OwnerHubDashboardOrdersCacheSeed.tsx` | `npm run measure:owner-dashboard-waterfall` | JSON·UI 레이아웃 불변 |
| 2026-05-21 | hub cold client wall breakdown (B) | **원인:** cold client ~900–1040ms — server handler ~935ms, `cm_unread_ms` ~686, `store_lookup` ~246; client−server ~100ms. **개선:** `[hub-cold-client-wall-breakdown]`·measure invalidate·`npm run measure:hub-cold-client-wall`. warm TTL 유지(76ms). **미달:** 300ms cold — cm_unread+linked RTT | `hub-cold-client-wall-breakdown.ts`, `store-owner-hub-badge/route.ts`, `measure-hub-cold-client-wall.mjs` | `npm run measure:hub-cold-client-wall` | order-counts/auth/waterfall 불변 |
| 2026-05-21 | order-counts cold RPC RTT breakdown (A) | **원인:** cold `rpc_wall_ms` ~210ms, parse/cache &lt;1ms — linked PostgREST RTT. **개선:** breakdown `auth_ms`·`rpc_transport_estimated_ms`·`cold_bottleneck_cause`·`primeStoreOrderCountsCache`·stores SB singleton · `measure:order-counts-cold-rpc` (cold 236 / warm 17–23). **미달:** 100ms cold — RTT 한계, SQL 미변경 | `order-counts-cold-breakdown.ts`, `order-counts-rpc-timing.ts`, `order-counts/route.ts`, `try-supabase-stores.ts`, `store-order-counts-cache.ts` | `npm run measure:order-counts-cold-rpc` | warm path·JSON·RPC SQL 불변 |
| 2026-05-21 | order-counts dashboard snapshot 1 RTT | **원인:** cold 직렬 auth→ownership→counts RPC→store_ops meta (~796ms). **개선:** `get_owner_store_ops_dashboard_snapshot` gate+meta+counts 단일 RPC·route ownership 제거·`[order-counts-cold-breakdown]`·`order_counts_via=rpc_snapshot` | `20260524120000_get_owner_store_ops_dashboard_snapshot_rpc.sql`, `fetch-owner-store-order-counts-dashboard-snapshot-rpc.ts`, `order-counts/route.ts` | `npm run measure:owner-dashboard-api` | hub badge·notifications·JSON 불변 |
| 2026-05-21 | owner-dashboard perf env SLO lock | **원인:** linked dev RTT로 cold 250ms 미달만 반복 튜닝. **개선:** 구조 vs latency SLO 분리·`measure:owner-dashboard-api` gate report·perf-v2 structural/rtt_limited | `docs/owner-dashboard-api-perf-lock.md`, `scripts/owner-dashboard-api-perf-gate.mjs`, `owner-dashboard-perf-v2.ts` | `npm run measure:owner-dashboard-api` | JSON·hub/notifications cache 불변 |
| 2026-05-21 | 오너 대시보드 hub no-store fast path + order-counts RPC | **원인**: find_hub rows=0 인데 unread_parts RPC ~400ms 병렬 실행; `get_owner_store_ops_snapshot_counts` 미배포 → legacy ~900ms. **개선**: find_hub 선행·no_hub 시 unread_parts/store_order skip(cm_unread만); find_hub empty singleflight; order-counts rpc-hit/legacy-fallback 로그·perf `fallback_used`/`order_counts_via`; notifications cache_hit 판정을 getCached 결과로 | `build-owner-hub-badge-payload.ts`, `user-chat-unread-parts.ts`, `fetch-owner-store-order-counts*.ts`, `notification-unread-count-cache.ts`, `order-counts/route.ts`, `notifications/route.ts` | `npm run measure:owner-dashboard-api`, `npx tsc --noEmit`, `npx supabase db push --include-all` | hub badge JSON·unread semantics·CM bootstrap 불변 |
| 2026-05-21 | 메신저 bootstrap `?lite=1` trade enrich | **원인**: `lite_trade_enrich_skipped` 는 mode 플래그만 true 인데 실제로는 trace 없이 full `enrichTradeRoomContextMetaForBootstrap` → posts schema fallback chain·Phase D peer 쿼리로 `trade_enrich_ms` ~2.8s. **개선**: lite 에 `tier:critical` trace + mega direct_keys + `fallback_only` category + Phase D 생략 + direct_keys 후 조기 종료; `[cm-bootstrap-breakdown]` 분해 필드 6종 추가 | `lib/community-messenger/service.ts`, `app/api/community-messenger/bootstrap/route.ts` | `npx tsc --noEmit` | lite 에서 posts fallback chain·`lite_trade_enrich_skipped` mode-only true 로 되돌리지 말 것 |
| 2026-05-21 | 메신저 bootstrap lite middle 제거 | **원인**: direct_keys 후에도 Phase A–C(middle ~780ms)·posts 519ms·seller 재실행. **개선**: `bootstrapLiteFastEnrich` 시 middle 차단·first-paint 충족 판정·잔여는 missing-only posts 1배치만; seller/category/peer-pair/Phase D 금지 | `lib/community-messenger/service.ts`, `app/api/community-messenger/bootstrap/route.ts` | `npx tsc --noEmit` | lite bootstrap 에 Phase A–C·seller hydrate 를 다시 넣지 말 것 |
| 2026-05-21 | 메신저 bootstrap lite direct_keys cold | **원인**: mega RPC ~351ms 가 trade_enrich 직렬 구간에만 겹쳐 cold FAIL. **개선**: rooms 직후 `Promise.all` 선행 prefetch·enrich 동일 Promise 재사용·mega posts in-RPC 재조회·category skip(bootstrap-lite); breakdown 로그 5종 | `lib/community-messenger/service.ts`, `app/api/community-messenger/bootstrap/route.ts` | `npx tsc --noEmit` | enrich 에서 mega RPC 를 다시 호출하거나 bootstrap-lite 에 category DB 를 다시 켜지 말 것 |
| 2026-05-21 | 메신저 bootstrap lite profiles hydrate | **원인**: lite 가 social+call+participant 전체 `allIds`(~43+)에 `profiles.in`(bio 포함) 1RTT → `profiles_query_ms` ~246ms. **개선**: bundle RPC `profile_labels` embed·lite는 viewer+participant만 hydrate·bio 제외·miss-only fetch·perf lock `profiles_query_ms` ≤50 | `supabase/migrations/20260522130000_community_messenger_bootstrap_lite_bundle_profile_labels.sql`, `lib/community-messenger/service.ts`, `bootstrap-lite-performance-lock.*`, `docs/messenger-bootstrap-lite-performance-lock.md` | `node scripts/_verify-bootstrap-lite-rooms-3run.mjs`, `npx tsc --noEmit` | lite first paint 에 social graph `profiles.in`·bio select 를 되돌리지 말 것 |
| 2026-05-21 | owner products perf lock + verify | **고정:** 구조 락·Run3 warm PASS·rewarm ≤50ms·cold/transitional WARN. **추가:** `owner-products-perf-lock.mjs`, `verify:owner-products-perf`, `owner-products-perf-lock.ts` dev warn | `docs/store-owner-products-perf-lock.md`, `scripts/verify-owner-products-perf.mjs` | `npm run verify:owner-products-perf` | slim snapshot·JSON 불변 |
| 2026-05-21 | owner products GET warm cache path | **원인:** list `cache_hit=1`인데 스냅샷에 cold `timing` 잔존·`validateActiveSession`+`getStoreIfOwner` 매 요청. **개선:** warm timing 0·`validateActiveSessionLightDeduped`·`getCachedStoreIfOwner`·list TTL 30s·perf 필드(`auth_cache_hit`,`early_return_from_cache`,…) | `products/route.ts`, `owner-products-list-snapshot.ts`, `owner-store-ownership-cache.ts` | warm `products_query_ms=0` | slim snapshot·JSON·invalidate 불변 |
| 2026-05-21 | owner products list slim snapshot | **원인:** 목록 GET PostgREST embed fan-out·전량 fetch ~1.1s. **개선:** embed 제거·카테고리/구역 배치 attach·4s cache+singleflight·`[owner-products-perf]`·RSC 동일 쿼리 | `products/route.ts`, `owner-products-list-snapshot.ts`, `owner-products-perf.ts`, `load-owner-store-read-bootstrap.ts` | dev `options_embed:0` | JSON 필드 키·정렬·UI 불변 |
| 2026-05-21 | auth/cm unread/owner orders hot path | **auth:** globalThis validate·registry·light snapshot TTL + `[auth-hot-path-breakdown]`. **cm_unread:** `hub_badge_user_unread_counters.community_messenger_unread_room_count` read-through + `[cm-unread-aggregate-perf]`. **owner orders:** server list cache 30s + buyer label cache + `[owner-orders-list-perf]`. JSON·unread semantics·RLS 불변 | `auth-*-cache.ts`, `community-messenger-unread-total.ts`, `owner-store-orders-list-server-cache.ts`, `orders/route.ts` | dev warm 3-run | bootstrap/mark_read 미변경 |
| 2026-05-21 | notifications unread segmented RPC + cache lock | **원인:** cold ~280ms·segmented mode별 2 head count·owner list 220건 slice. **개선:** `count_notification_unread_segmented` 1 RTT·globalThis TTL/singleflight·`get_owner_store_commerce_notifications`·perf `notifications_via` | `20260525100000_notification_unread_segmented_rpc.sql`, `notification-unread-count-cache.ts`, `notifications/route.ts` | `npm run measure:owner-dashboard-api` | unread semantics·JSON 불변 |
| 2026-05-21 | store order detail snapshot RPC | **원인:** buyer/owner GET 5+ PostgREST RTT(800~1500ms linked). **개선:** `get_buyer/owner_store_order_detail_snapshot` 1 RTT·auth∥RPC·`snapshot_via`·legacy fallback | `20260525101000_store_order_detail_snapshot_rpcs.sql`, `fetch-store-order-detail-snapshot-rpc.ts` | dev `[store-order-detail-perf] snapshot_via=rpc_snapshot` | JSON·ensure GET 금지 불변 |
| 2026-05-21 | store order detail GET read-only | **원인:** buyer GET이 `ensureStoreOrderMessengerRoom`+summary를 매 조회 동기 실행(10+ RTT, 4~9s). **개선:** GET read-only snapshot·`[store-order-detail-perf]`·`POST .../ensure-chat` mutation·owner GET 동일·슬라이드 패널 ensure-chat POST | `store-orders/[orderId]/route.ts`, `ensure-chat/route.ts`, `store-order-detail-perf.ts`, `OwnerStoreOrderChatSlidePanel.tsx`, `docs/store-order-detail-perf-lock.md` | dev GET `ensure_skipped:1` | JSON·unread·realtime·UI·POST create ensure 불변 |
| 2026-05-21 | CM PATCH mark_read 구조 lock | **원인**: permission cold `service.ts` dynamic import + 번들별 membership Map miss (~699ms). **개선**: `canonical-resolve-core`·globalThis membership cache·bootstrap/GET seed·combined open_tail 1RTT 유지·`[cm-mark-read-perf-lock]`·`docs/messenger-mark-read-performance-lock.md` | `messenger-room-canonical-resolve-*`, `mark-read-performance-lock.*`, `rooms/[roomId]/route.ts`, `scripts/_verify-mark-read-permission.mjs` | `node scripts/_verify-mark-read-permission.mjs` | combined RPC SQL·unread·JSON·realtime·warm duplicate 경로 불변; local_linked RTT WARN ≠ 구조 FAIL |
| 2026-05-21 | DIBAY owner hot path verify fixes | **관측·연동만:** auth dedupe warm `[auth-hot-path-breakdown]`·CM aggregate perf 필드·hub TTL 시 memory 관측·PATCH 후 `invalidateOwnerStoreOrdersListCache`·`[owner-orders-list-cache-invalidate]`·instrumentation `[dev-module-graph]`. JSON·unread·orders warm cache·bootstrap 불변 | `auth-session-validate-dedupe.ts`, `cm-unread-*`, `orders/[orderId]/route.ts`, `instrumentation.ts` | dev 3-run auth/orders/hub | owner orders list warm path·semantics 불변 |
| 2026-05-24 | store order chat entry timeline | **원인:** (1) peek 빈 캐시가 ensure full history 시드보다 먼저 적용 (2) virtualizer viewport 미부착 시 0행 (3) 오너 슬라이드 effect가 `order.community_messenger_room_id` 갱신마다 RoomClient 리셋 (4) 구매자 URL bootstrap+ensure 2왕복. **개선:** `pickAuthoritativeMessengerRoomSnapshot`·ensure `roomSnapshot` full history·direct timeline fallback·슬라이드 deps 고정·cache-hit ensure 1왕복·`verify:store-order-messenger-room-entry` | `store-order-messenger-room-entry-client.ts`, `messenger-room-initial-snapshot-authority.ts`, `store-order-ensure-chat-with-bootstrap.ts`, `OwnerStoreOrderChatSlidePanel.tsx`, `CommunityMessengerRoomPhase2MessageTimeline.tsx` | `npm run verify:store-order-messenger-room-entry`, unit tests | ensure 응답 `roomSnapshot`·슬라이드 deps·시드 authority 불변 |
| 2026-05-24 | 배달 주문 채팅 타임라인 하단 정렬 | **원인:** fallback 가상행 absolute + `useDirectTimelineLayout`이 fallback 시 항상 false → 진입 빈 화면. **개선:** `resolveUseDirectMessengerTimelineLayout`·주문 방 전량 direct·`scheduleMessengerScrollToBottomAfterRowsPainted`(중복 rAF coalesce)·진입 스크롤 delivery=timeline / 그외=reader·정적 verify·unit test | `messenger-timeline-layout-mode.ts`, `CommunityMessengerRoomPhase2MessageTimeline.tsx`, `use-messenger-room-reader-scroll-bottom.ts`, `use-messenger-room-store-order-dock-scroll-anchor.ts`, `tests/unit/messenger-timeline-layout-mode.test.ts`, `verify-store-order-messenger-room-entry-contract.cjs` | `npx tsc --noEmit`, `npm run verify:store-order-messenger-room-entry`, vitest layout-mode | `cappedVirtualRows.length===0`·mt-auto·조기 scrollTop 복귀 금지 |
| 2026-05-24 | 배달·주문 채팅 UI (헤더·하단·토큰) | **UX:** 역할별 Phase2 헤더(매장→주문자·구매자→매장)·슬라이드 배너·툴바 제거·하단 `DeliveryChromeStrip` 단일 주문 메타·Starbucks `delivery-tokens`→`--cm-room` 브리지·매장 운영은 「주문」 드로어만. **타임라인 스크롤 계약 분리 유지** | `CommunityMessengerRoomPhase2Header.tsx`, `OwnerStoreOrderChatSlidePanel.tsx`, `CommunityMessengerRoomPhase2StoreOrderChrome.tsx`, `delivery-tokens.css`, `design-tokens.css`, `messenger-delivery-room-header.ts`, `verify-store-order-messenger-room-entry-contract.cjs` | `npx tsc --noEmit`, `npm run verify:store-order-messenger-room-entry`, vitest delivery-header + layout-mode | direct timeline·dock anchor·ensure 시드 불변 |
| 2026-05-24 | 배달·주문 점세개·매장명 | **UX:** 배달 방 점세개 영상통화 숨김·구매자 메뉴=매장 프로필+주문 요약·`resolveDeliveryStoreDisplayName`(slug/UUID 제목 폴백 금지)·시스템 행 Starbucks 톤 | `ChatRoomMoreMenu.tsx`, `CommunityMessengerRoomPhase2OneToOneDotMenu.tsx`, `messenger-delivery-room-header.ts`, `MessengerTimelineVirtualRow.tsx` | vitest delivery-header, `npx tsc --noEmit` | 거래 채팅 dot 메뉴·통화 정책 불변 |
| 2026-05-24 | 배달·주문 채팅 즉시 셸 | **원인:** BootstrapGate·오너 슬라이드가 ensure+bootstrap 완료까지 RoomClient 미마운트·「연결 중」문구. **개선:** `resolveInstantStoreOrderMessengerEntrySnapshot`·목록 `community_messenger_room_id` 즉시 셸·ensure `onShellReady`(bootstrap fetch 전)·타임라인 `StoreDeliveryBufferingSpinner` | `store-order-messenger-entry-shell-snapshot.ts`, `store-order-messenger-room-entry-client.ts`, `OwnerStoreOrderChatSlidePanel.tsx`, `CommunityMessengerRoomBootstrapGate.tsx`, `CommunityMessengerRoomPhase2MessageTimeline.tsx` | `npx tsc --noEmit`, `npm run verify:store-order-messenger-room-entry` | ensure full history·슬라이드 deps·direct timeline 불변 |
| 2026-05-25 | 오너 허브 배지 snapshot architecture | **원인:** warm TTL 의존·cold multi-RPC wave(843ms)·embed inner join·sequential await 재발. **개선:** `get_owner_hub_badge_snapshot` 1 RTT·`tryBuildOwnerHubBadgeFromSnapshot` counter PK read-first·event `scheduleOwnerHubBadgeSnapshotRefresh`·regression guard·`docs/perf/hub-badge-regression-lock.md`. embed join 제거·wave parallelized·`query_wave_2_ms=0`. **판정:** structural PASS — legacy fallback 미사용 확인 | `20260525180000_owner_hub_badge_snapshot_rpc.sql`, `owner-hub-badge-snapshot.ts`, `hub-badge-snapshot-refresh.ts`, `hub-badge-regression-guard.ts`, `build-owner-hub-badge-payload.ts` | `verify:hub-badge-snapshot-rpc`, `verify:hub-badge-snapshot-e2e`, `measure:owner-hub-badge-perf` | JSON·unread semantics·bootstrap/realtime 불변; local apply 재현·owner 12 시나리오·prod same-region ▲ 후속 |
| 2026-05-25 | OOL1 owner orders list snapshot-first | **원인:** NHR1 hot route — 2-wave Promise.all(orders + buyer labels + items + reviews) request-time aggregate ~3s cold. **개선:** `get_owner_store_orders_list_snapshot` 1 RTT·`tryLoadOwnerStoreOrdersListFromSnapshot` counter PK read-first·event `scheduleOwnerStoreOrdersListSnapshotRefresh` via `invalidateOwnerStoreOrdersListCache`/`invalidateStoreOrderCountsCache`·regression guard·`docs/perf/owner-orders-list-regression-lock.md`. **판정:** structural PASS — fallback **0** · `query_wave_2_ms=0` · `rpc_removed=1` | `20260526000000_owner_store_orders_list_snapshot.sql`, `owner-store-orders-list-snapshot*.ts`, `fetch-owner-store-orders-list-legacy.ts`, `orders/route.ts` | `verify:owner-orders-list-snapshot-rpc`, `verify:owner-orders-list-snapshot-e2e`, `npx tsc --noEmit` | JSON·sort·pagination·filter semantics·UI 불변; ▲ legacy fallback 제거 · ▲ prod same-region · ▲ 수동 UI 12시나리오 |
| 2026-05-25 | CMB1 CM bootstrap lite snapshot-first | **원인:** NHR1 #2 hot route — bootstrap monolith multi-wave (rooms + profiles + unread + trade enrich) ~450ms+ cold. **개선:** `get_cm_bootstrap_critical_snapshot` 1 RTT·`tryLoadCmBootstrapLiteFromSnapshot` counter PK read-first·event `scheduleCmBootstrapSnapshotRefresh` via `invalidateCmBootstrapSnapshotCache` (message/read/participant/mark-all-read)·regression guard·`docs/perf/cm-bootstrap-regression-lock.md`. **판정:** structural PASS — fallback **0** · `query_wave_2_ms=0` · `rpc_removed=1` | `20260526100000_cm_bootstrap_critical_snapshot.sql`, `cm-bootstrap-snapshot*.ts`, `fetch-cm-bootstrap-legacy.ts`, `bootstrap/route.ts` | `verify:cm-bootstrap-snapshot-rpc`, `verify:cm-bootstrap-snapshot-e2e`, `npx tsc --noEmit` | JSON·unread·room ordering·UI 불변; ▲ legacy fallback 제거 · ▲ full tier snapshot · ▲ prod same-region |
| 2026-05-25 | CR1 trade chat rooms list snapshot-first | **원인:** NHR1 #4 hot route `/api/chat/rooms` — 7-wave monolith (product_chats + participants + chat_rooms + messages + posts + profiles + orders) request-time aggregate DANGER. **개선:** `get_chat_rooms_snapshot` 1 RTT·`tryLoadChatRoomsFromSnapshot` counter PK read-first·event `scheduleChatRoomsSnapshotRefresh` via `invalidateChatRoomsSnapshotCache` (message/read/participant/hub-badge/mark-all-read)·regression guard·`docs/perf/chat-rooms-regression-lock.md`. **판정:** structural PASS — fallback **0** · `query_wave_2_ms=0` · `rpc_removed=1` | `20260526200000_chat_rooms_snapshot.sql`, `chat-rooms-snapshot*.ts`, `chat-rooms-list-core.ts`, `fetch-chat-rooms-list-legacy.ts`, `chat/rooms/route.ts` | `verify:chat-rooms-snapshot-rpc`, `verify:chat-rooms-snapshot-e2e`, `npx tsc --noEmit` | JSON·unread·room ordering·segment semantics·UI 불변; ▲ legacy fallback 제거 · ▲ prod same-region · ▲ 수동 UI 15시나리오 |
| 2026-05-25 | FBT1 CM bootstrap full tier snapshot-first | **원인:** CMB1 이후에도 full bootstrap + `?tier=critical` 가 legacy monolith multi-wave( social + discoverable + calls + trade enrich ) request-time aggregate. **개선:** `get_cm_bootstrap_full_snapshot(p_tier)` 1 RTT·scopes `full_monolith`/`critical_tier`·`tryLoadFullBootstrapFromSnapshot`/`tryLoadCriticalBootstrapFromSnapshot`·`invalidateFullBootstrapSnapshotCache`·regression guard·`docs/perf/full-bootstrap-regression-lock.md`. **판정:** structural PASS — fallback **0** · `query_wave_2_ms=0` · `rpc_removed=1` | `20260526240000_cm_bootstrap_full_snapshot.sql`, `full-bootstrap-snapshot*.ts`, `fetch-full-bootstrap-legacy.ts`, `bootstrap/route.ts` | `verify:full-bootstrap-snapshot-rpc`, `verify:full-bootstrap-snapshot-e2e`, `npx tsc --noEmit` | JSON·unread·room ordering·tier semantics·UI 불변; CMB1 lite scope 재개 금지; ▲ legacy fallback 제거 · ▲ prod same-region |
| 2026-05-25 | LFC1 legacy fallback cleanup infrastructure | **원인:** PASS snapshot-first 트랙에 legacy multi-wave fallback branch 잔존 — 복잡도·reconnect edge·hidden aggregate 회귀 위험. **개선:** `LEGACY_FALLBACK_ROUTE_REGISTRY` · extended audit · `SAMARKET_LFC1_SNAPSHOT_ONLY` soft-disable · regression guard · cleanup lock/report · verify scripts. **판정:** step 1–2 진행 — hard delete **OPS1-B gate blocked** | `legacy-fallback-cleanup-policy.ts`, `legacy-fallback-usage-audit.ts`, `legacy-fallback-cleanup-regression-guard.ts`, `fetch-*-legacy.ts` gate wiring | `verify:legacy-fallback-cleanup-audit`, `verify:legacy-fallback-cleanup-structural` | default behavior unchanged; ▲ per-route hard delete · ▲ OPS1-B sign-off |
| 2026-05-25 | store menus route memory SWR | **원인:** warm reopen(15–45s)마다 snapshot row SELECT ~130ms blocking — route memory 45s hard TTL 만료 후 DB lookup 재발. **개선:** soft 15s / hard 60s SWR·soft stale 즉시 return + `scheduleStoreMenusRouteMemoryRevalidate` fire-and-forget·`[snapshot-swr-analysis]` 계측·mutation은 memory invalidate only 유지 | `store-menus-public-server-cache.ts`, `snapshot-swr-analysis.ts`, `app/api/stores/[slug]/menus/route.ts` | vitest `store-menus-route-memory-swr`, `npx tsc --noEmit` | JSON·menu semantics·UI·snapshot row 제거 불변; stale ≤60s |
| 2026-05-25 | store menus hard-stale pre-refresh | **원인:** 단발 방문 후 60s+ idle 시 hard stale snapshot row ~220ms blocking — soft SWR만으로는 반복 방문 없으면 회피 불가. **개선:** hard TTL 직전 proactive timer refresh·soft stale refresh 성공 시 hard TTL 재연장·inflight dedupe·`[snapshot-pre-refresh]` 계측 — hard stale 경로 유지 | `store-menus-public-server-cache.ts`, `snapshot-pre-refresh.ts`, `menus/route.ts` | vitest pre-refresh timer, `npx tsc --noEmit` | JSON·semantics·hard stale 제거 불변·stale 무한 금지 |
| 2026-05-27 | C2C 거래·채팅 마무리 | **개선:** 표준 계측 키 `trade-c2c-perf-metrics`(디버그·perf 플래그만)·compose/createOrGet i18n·mark-read order 캐시 4k 상한·trade presence realtime subscribe/unsubscribe 카운터. 핫패스(compose 비대기·방 중복 방지) 불변 | `trade-c2c-perf-metrics.ts`, `TradeChatComposeClient.tsx`, `createOrGetChatRoom.ts`, `mark-read-duplicate-fast-path.ts`, `use-trade-chat-room-presence.ts`, `chats.ts` | `verify:trade-hot-path-contract`, `check:i18n`, `tsc` | compose 즉시 replace·RSC related·동기 createOrGet 금지 |
| 2026-05-27 | 상세 i18n·레거시 chats | **개선:** `PostDetailView`·`TradeListingStatusBadge` 전면 i18n(`trade_detail_*`·기존 `ui_*`/`mypage_comp_*` 재사용)·`/chats/[roomId]` 거래 미연동 CM 시 목록 redirect·홈 visible trade realtime 구독 24 cap. **문서:** `docs/trade-c2c-perf-baseline.md` | `PostDetailView.tsx`, `trade.ts`, `post-detail-i18n.ts`, `seller-listing-i18n.ts`, `chats/[roomId]/page.tsx`, `trade-realtime-subscribe-policy.ts` | `check:i18n`, `check:i18n-hardcoded` PostDetailView | 핫패스·UI shape 불변; store_order 비CM 레거시 `/chats` 유지 |
| 2026-05-27 | 거래 목록 realtime P3 | **개선:** `/trade-chats` viewport visible-only subscribe·`TRADE_ROOM_REALTIME_UNSUBSCRIBE_DEBOUNCE_MS`(450) debounce unsubscribe·active route room pin·cap 24은 home route 제외 merge·목록 reorder 시 fingerprint 안정·채널 bind 전체 fingerprint blind cap 제거. **계측:** duplicate/visible/debounce/pinned 카운터. UI·CM read/unread 불변 | `trade-visible-room-realtime-registry.ts`, `trade-c2c-perf-metrics.ts`, `use-messenger-room-list-prefetch-intersection.ts`, `CommunityMessengerHome.tsx`, `community-messenger-home-realtime-channels.ts` | `verify:trade-hot-path-contract`, `tsc` | home bootstrap roomIds·HS4 reorder-only coalesce·compose 비대기 불변 |
| 2026-05-27 | 거래 채팅 진입(resolve) | **원인:** `ensureCommunityMessengerDirectRoomFromProductChat` 가 `hydrateTradeMessengerRoomSummaryFromProductChat`(posts·category·profiles·`updateCommunityMessengerRoomContextMeta`)를 entry resolve 응답 전에 동기 await — `trade_chat_open_total_ms`·`trade_chat_bootstrap_ms` 대부분. **개선:** `ensureMessengerRoomIdForItemTrade` 만 `deferSummaryHydration` → `after()` (`product_chats`·`chat_rooms` FK 는 동기). bridge·`POST …/rooms` 는 동기 hydrate 유지. **계측:** `trade_chat_resolve_ms`·`trade_chat_redirect_ms`·`cm_room_bootstrap_*` | `service.ts`, `ensure-messenger-room-for-trade-chat.ts`, `trade-c2c-perf-metrics.ts`, `createOrGetChatRoom.ts`, `TradeChatComposeClient.tsx`, `fetch-community-messenger-room-bootstrap-client.ts` | `verify:trade-hot-path-contract`, `tsc`, `node scripts/capture-trade-c2c-baseline.mjs` | CM bootstrap JSON·read/unread·UI 불변; entry 응답 전 summary hydrate 동기 복귀 금지 |
| 2026-05-27 | 거래 채팅 진입(resolve) 2차 | **원인:** 기존 `item_trade` 방 재진입 시 `chat_rooms`·`product_chats`·CM id 가 이미 맞는데도 `ensureMessengerRoomIdForItemTrade`→`ensureCommunityMessengerDirectRoom` 전체 재실행(순차 RTT). **개선:** 참가자·`chat_rooms`·`product_chats` 최소 컬럼 **병렬 3 RTT** + CM id 정합 시 **ensure 생략**(PC FK 비었을 때만 1회 update). `TRADE_ENTRY_PERF_LOG` 분해 키 추가. | `item-trade-chat-start-core.ts`, `ensure-messenger-room-for-trade-chat.ts`, `trade-entry-perf-log.ts` | `verify:trade-hot-path-contract`, `node scripts/measure-trade-entry-resolve.mjs` | 중복 방 생성·FK 동기·hydrate defer·response shape 불변 |
| 2026-05-27 | 거래 채팅 진입(E2E) | **원인:** `openCreateTradeChat` 이 compose 로만 이동한 뒤 `TradeChatComposeClient` mount 에서야 resolve 시작 → compose dev compile + resolve 직렬. **개선:** 클릭 직후 `prepareTradeChatRoom`(inflight resolve) 를 **compose `replace` 전**에 호출. **계측:** `trade-chat-entry-journey-perf` 구간 키 + baseline 스크립트 room URL·shell 대기. UI·resolve API·read/unread·shape 불변 | `trade-chat-entry-journey-perf.ts`, `trade-chat-entry-navigation.ts`, `createOrGetChatRoom.ts`, `TradeChatComposeClient.tsx`, `capture-trade-c2c-baseline.mjs` | `capture-trade-c2c-baseline.mjs`, `measure-trade-entry-resolve.mjs` | resolve 서버 핫패스 재수정 금지 |
| 2026-05-27 | 거래 채팅 room route prefetch | **원인:** compose 에서 `void router.prefetch(dest)` 직후 `router.replace` → RSC cold 가 `router_replace_to_room_url_ms`(~12.6s)에 합산. **개선:** resolve 직후 `scheduleTradeHubRoomRoutePrefetch`·compose mount 시 선행 prefetch·`prefetchTradeHubRoomRouteBeforeNavigate` 가 RSC `_rsc=` flight 완료까지 대기(12s cap)·href TTL 중복 금지·**replace 는 prefetch 후**. **계측:** `room_prefetch_*`·`resolve_done_to_prefetch_start_ms`·`prefetch_done_to_router_replace_ms`. UI·resolve API·read/unread·shape 불변 | `trade-chat-room-route-prefetch.ts`, `TradeChatComposeClient.tsx`, `createOrGetChatRoom.ts`, `trade-chat-entry-journey-perf.ts`, `trade-c2c-perf-metrics.ts`, `capture-trade-c2c-baseline.mjs` | `verify:trade-hot-path-contract`, `capture-trade-c2c-baseline.mjs`, `measure-trade-entry-resolve.mjs` | fire-and-forget prefetch+즉시 replace 복귀 금지 |
| 2026-05-28 | 거래 1차 탭 440ms push | **원인:** `trade-primary` 는 intent 탭만 즉시·본문은 canonical 동일 index→subtle(150ms) 또는 `loading.tsx`/overlay 스켈레톤. **개선:** `commitTradePrimaryTabRoute`(탭 인덱스 push 축·2rAF navigate)·`AppRouteTransition` dual-panel에 `trade-primary` 포함·`TradeMarketTabPushEnterPanel`(peek 셸)·`market/loading` null·전면 스켈레톤 제외 유지. **계약:** `.cursor/rules/trade-primary-tab-transition-contract.mdc` | `commit-trade-primary-tab-route.ts`, `TradePrimaryTabs.tsx`, `AppRouteTransition.tsx`, `MainShellTabContentTransition.tsx`, `TradeMarketTabPushEnterPanel.tsx`, `HomeProductList.tsx` | `tsc`, vitest `compute-trade-primary-push-axis` | Link만으로 navigate·`bottom-nav`만 dual-panel·canonical 축으로 마켓 서브탭 push 금지 |
| 2026-05-28 | 거래 1차 탭 더블체크 | **누락:** 피드 스와이프가 `router.push` 만 사용해 440ms·intent 우회. **오류:** `tab.isActive` 가 pending 중 재클릭·`fromTabIndex -1` push 미시작. **개선:** `commitTradeSwipeTabRoute`·탭 클릭 가드·`pathnameActiveIndex` fallback·`verify:trade-primary-tab-transition`·계약 매트릭스 | `commit-trade-swipe-tab-route.ts`, `MarketContent.tsx`, `MarketCategoryFeed.tsx`, `TradePrimaryTabs.tsx`, `trade-primary-tab-transition-contract.mdc` | `verify:trade-primary-tab-transition`, `tsc` | 스와이프·탭 클릭 이중 경로·스와이프 경계를 trade-primary push 로 통일 금지 |
| 2026-05-28 | 거래 탭 스켈레톤 회귀 | **원인:** 440ms push 중 `AppRouteTransition` 이 pathname 도착 시 `entering` 을 `children`(Suspense·`MainFeedRouteLoading`)로 교체 + `/market` `Suspense` fallback. **개선:** push 활성 시 entering 유지·`trade-primary` 항상 `TradeMarketTabPushEnterPanel`·`market/page` 즉시 셸·`MarketCategoryRouteFallback` 스켈레톤 제거·`HomeProductList` 클라 peek | `AppRouteTransition.tsx`, `market/page.tsx`, `MarketCategoryRouteFallback.tsx`, `HomeProductList.tsx` | `verify:trade-primary-tab-transition`, `tsc` | push 중 `entering: children` 복귀·`/market` blocking Suspense 복귀 금지 |
| 2026-05-28 | 거래 홈 캐시 지속 | **원인:** 홈 피드 캐시 TTL(45s) 만료 시 매번 스켈레톤 반복. **개선:** `localStorage` 24h 캐시 추가 → 메모리·session 미스 시에도 즉시 목록 복원, 백그라운드 refresh. | `getPostsForHome.ts` | `tsc` | local cache 삭제 로직 누락 금지 |
| 2026-05-28 | 거래 스켈레톤 렌더 차단 | **원인:** bottom-nav → `/market` pending panel 이 아직 `MainFeedRouteLoading` 을 사용하고, `HomeProductList` 자체 loading state 도 카드 스켈레톤을 렌더. **개선:** `/market` intent 는 source 와 무관하게 `TradeMarketTabPushEnterPanel`, 홈 loading 은 비시각 빈 배경, verify 스크립트에 회귀 금지 추가 | `MainShellTabContentTransition.tsx`, `HomeProductList.tsx`, `verify-trade-primary-tab-transition-contract.cjs` | `verify:trade-primary-tab-transition`, `tsc` | `/market` 에서 카드 skeleton loading state 복귀 금지 |
| 2026-05-28 | 메인 메뉴 feed skeleton 제거 | **원인:** `MainShellTabContentTransition` 의 feed pending 기본값이 `MainFeedRouteLoading` 이라 source/target 조건 누락 시 카드 스켈레톤이 재등장 가능. **개선:** feed pending shell 을 `null` 로 고정, messenger 만 전용 shell 허용, 검증 스크립트에서 import 자체 금지 | `MainShellTabContentTransition.tsx`, `verify-trade-primary-tab-transition-contract.cjs` | `verify:trade-primary-tab-transition`, `tsc` | menu transition 에 `MainFeedRouteLoading` 재도입 금지 |
| 2026-05-28 | 하단 5탭 skeleton 전면 금지 | **범위 정정:** `/market` 만이 아니라 커뮤니티·거래·배달·메신저·내정보 전체 이동. **개선:** `MainShellTabContentTransition` 에서 messenger skeleton 포함 모든 pending shell 제거, 5탭 root `loading.tsx` 는 `null`, verify 가 각 root loading 의 skeleton/`animate-pulse` 금지 확인 | `MainShellTabContentTransition.tsx`, `philife/loading.tsx`, `market/loading.tsx`, `stores/loading.tsx`, `community-messenger/loading.tsx`, `mypage/loading.tsx`, `my/loading.tsx`, `verify-trade-primary-tab-transition-contract.cjs` | `verify:trade-primary-tab-transition`, `tsc` | 하단 5탭 이동에 skeleton shell 재도입 금지 |
| 2026-05-28 | 하단 5탭 pending enter 안정화 | **원인:** `/market` 외 하단 탭은 `pendingPushNode` 가 없어 440ms push 시작 시 들어오는 패널이 현재 화면/route fallback 으로 잡히고, push 종료 뒤 실제 children 으로 교체되며 깜빡임·늦게 뜸이 발생. **개선:** 커뮤니티·배달·메신저·내정보도 목적지 클라 허브를 pending enter panel 로 즉시 마운트하고, 루트 Suspense/loading/client loading 의 카드 스켈레톤을 빈 안정면으로 교체 | `MainShellTabContentTransition.tsx`, `CommunityFeed.tsx`, `StoresHomeHub.tsx`, `CommunityMessengerHomeListPane.tsx`, `community-messenger/page.tsx`, `mypage/page.tsx`, `stores/loading.tsx`, `verify-trade-primary-tab-transition-contract.cjs` | `verify:trade-primary-tab-transition`, `verify:stores-home-hub-contract`, `tsc` | `/market` 전용 pending panel 로 축소·루트 탭 카드 스켈레톤 fallback 복귀 금지 |
| 2026-05-28 | 하단 5탭 방향 축 통일 | **원인:** `ltr/rtl` 의미가 dual-panel 과 `(stores)` remount surface 에서 반대로 쓰여 배달·메신저처럼 route group 경계를 지나는 탭에서 좌측→우측 후 반대쪽 재진입처럼 보임. **개선:** 사용자 기준으로 축 통일(좌측 탭=ltr 좌→우, 우측 탭=rtl 우→좌), trade 1차 탭도 동일 기준, push 종료 후 1.2s handoff overlay 로 첫 접속 child 교체 하얀 화면 가림 | `route-transition-enter-kind.ts`, `route-transition-config.ts`, `AppRouteTransition.tsx`, `globals.css`, `compute-main-bottom-nav-push-axis.ts`, `compute-trade-primary-push-axis.ts`, 관련 테스트 | `verify:trade-primary-tab-transition`, route transition vitest, `verify:parity-gates`, `build` | `ltr=우측`/`rtl=좌측` 의미 복귀·cross-group surface 방향 반전 금지 |
| 2026-05-30 | 앱 부트 profile lite | **원인:** lite SELECT 성공 후 `mergeOptionalFields` 2nd DB RTT → `profile_row_normalize_ms` ~341ms·total ~604ms. **개선:** `SELECT_ME_PROFILE_LITE` hit 시 merge 생략·map-pin 주소는 background full profile. **LOCK:** [dibay-app-boot-api-perf-lock.md](./dibay-app-boot-api-perf-lock.md) §1 | `lib/profile/fetch-profile-row-safe.ts` | PM2 cold/warm·`verify:app-boot-consent-contract` | lite merge await 복귀 금지 |
| 2026-05-30 | 앱 부트 hub badge cm_unread | **원인:** cold `cm_unread_ms` ~335ms·total ~362ms cache miss. **개선:** memory TTL 12s·aggregate stale SWR·singleflight dedupe. unread semantics·JSON 불변 | `cm-unread-room-count-memory-cache.ts`, `cm-unread-room-count-aggregate.ts`, `community-messenger-unread-total.ts` | PM2·aggregate path | TTL·semantics 변경은 별도 승인 |
| 2026-05-30 | 앱 부트 notifications index | **원인:** cold `count_notification_unread_segmented` ~480ms (segment filter·partial index 미스). **개선:** consumer_no_chat·bottom_nav_no_chat partial index 2개·RPC/cache/route 불변 | `20260830120000_notification_unread_segmented_badge_indexes.sql` | PM2 cold/warm·SQL Editor index 확인 | index drop·RPC rewrite 별도 승인 |
| 2026-05-30 | 앱 부트 auth/session parallel | **원인:** `validateActiveSessionLight` profile SELECT → registry validate 직렬 → auth_total ~204–246ms. **개선:** `Promise.all` 병렬·판정 동일·`auth_validate_ms=max`. TTL 미변경 | `lib/auth/server-guards.ts` | PM2 `[auth-hot-path-deep]` | registry/session 검증 제거·TTL 무승인 변경 금지 |
| 2026-05-30 | usable-first 3도메인 | **원인:** shell은 빠�나 cold gate가 list usable blocking — 메신저 prewarm lite≠critical tier, 배달 home-feed session·in-flight 미합류, 거래 prewarm memory-only skip·440ms push cache-hit 체감. **개선:** critical prewarm+minimal peek seed; stores sessionStorage 45s+in-flight no skeleton; trade `peekCachedPostsForHome` prewarm·cache-hit push 250ms | `warm-messenger-list-bootstrap-client.ts`, `use-community-messenger-home-bootstrap.ts`, `store-home-feed-client-cache.ts`, `StoresHomeHub.tsx`, `bottom-nav-tap-prewarm-trade.ts`, `resolve-main-shell-push-duration-ms.ts`, `AppRouteTransition.tsx` | `verify:trade-primary-tab-transition`, `verify:stores-home-hub-contract`, nav-perf | global prefetch·home-sync rewrite·unread semantics 변경 금지 |
| 2026-05-30 | nav-perf Confirm gate Phase A | **원인:** read-only hub 교차·메신저 진입마다 교육용 Cross-domain/Messenger Confirm이 route/push를 +600~900ms blocking — prewarm/cache는 준비됐으나 `requestTransition`이 commit hold. **개선:** `resolveBottomNavTransitionConfirmCopy` safe navigation(null)·risky probe scaffold·write/cart/checkout guard 유지 | `main-bottom-nav-risky-navigation.ts`, `main-bottom-nav-transition-copy.ts`, `BottomNav.tsx`, `main-bottom-nav-interaction-contract.ts` | `verify:delivery-dial-navigation-contract`, `verify:trade-primary-tab-transition`, nav-perf | tab id 우회·write guard 제거·i18n key 삭제 금지 |
| 2026-05-30 | nav-perf Confirm Phase A 검증 lock | **수동(390×844 mobile, qqqq@manual.local):** hub 교차 7경로 `confirmShown=false`·`confirm_to_commit` 0건. **대표 nav-perf:** philife→trade `clickToIntentMs=0`·`firstShellVisibleMs=449`·`routeSettledMs=400`; market→community `routeSettledMs=1107`·`firstShellVisibleMs=1849`. write sheet dirty 자동 E2E는 시트 진입 UI 미확정 — **코드 경로** `guardBeforeNavigate` 선행 유지. **LOCK:** [dibay-performance-lock.md](./dibay-performance-lock.md) §1.2 T-L5 | 동일 + `use-inline-write-sheet-navigation-guard.ts` | Playwright mobile nav-perf | read-only cross-domain Confirm blocking 복귀 금지 |
| 2026-05-30 | Phase A FINAL LOCK | **확정:** safe nav 7/7·tsc·lint·verify trade/stores hub·측정 스크립트 `bn6_phase_a_safe_nav`. **후속:** `MessengerHomeDebugEvent`+`messenger_home_warm_critical_success`(tsc TS2345). **미구현(승인 전):** Phase B cart/checkout/upload/chat probe | `measure-bottom-nav-confirm-immediacy.mjs`, `samarket-runtime-debug.ts`, T-L5 lock | `node scripts/measure-bottom-nav-confirm-immediacy.mjs`, tsc, lint, verify:* | Phase B·read-only Confirm blocking 복귀 금지 |
| 2026-06-12 | 하단 셸·배민 `/stores` 배지 poll | **원인:** Tier1 종 surface store 가 pathname 과 무관하게 75s poll·Realtime INSERT 시 전 surface refresh — `/stores` 에서 `bottom_nav_chat` poll·네트워크 경합. **개선:** `reconcileTier1BellSurfacePolling`·active surface refresh only·hub badge `hasOwnerStoreRef` 로 탭별 trace 분리 | `notification-unread-badge-store.ts`, `NotificationsBadgeRealtimeBridge.tsx`, `MessagingGlobalChrome.tsx`, `use-owner-hub-badge-total.ts` | vitest notification-unread-badge-store, `tsc` | `/stores` 에서 비 delivery surface poll 재개·전 surface INSERT refresh 복귀 금지 |
| 2026-06-12 | 배민 `/stores` 메신저 ambient prewarm | **원인:** 배달 허브 `/stores` 체류 중 chat 탭 hover/pointerdown 이 `prewarmBottomNavMessengerTab` → CM bootstrap 청크·layout.css preload·longtask 경합. **개선:** `shouldDeferMessengerPrewarmOnDeliverySurface` — `pointer_intent`/`idle` skip, `route_commit`/`boot` 만 warm | `bottom-nav-tap-prewarm-data.ts`, `main-bottom-nav-route-commit.ts`, `BottomNav.tsx` | vitest bottom-nav-tap-prewarm-data, `tsc` | 탭 commit 후 메신저 첫 진입 warm 제거 금지 |
| 2026-06-12 | CM 진입 시 stores prewarm 잔여 | **원인:** `/stores` viewport·pointer ambient prewarm(menus/summary) in-flight 가 chat 탭 전환 후에도 완료·trace. **개선:** `delivery-store-detail-prewarm-lifecycle` AbortSignal·browse hub pathname 가드·`commitMainBottomNavRoute` 이탈 abort·`delivery-fetch-trace` pathname | `delivery-store-detail-prewarm-lifecycle.ts`, `store-delivery-api-client.ts`, `delivery-store-*-prewarm.ts`, `main-bottom-nav-route-commit.ts` | vitest prewarm-lifecycle·route-commit, `tsc` | 카드 `force` 상세 prewarm·CM bootstrap/warm·Realtime poll 변경 금지 |
| 2026-06-12 | CM bridge static graph 분리 BN12-A | **원인:** `use-message-notification-bridge` 정적 import 가 room-bootstrap·forward-nav·call/banner store 를 root 에 묶음. **개선:** `useCmParticipantsHubSync` static core + `cm-participant-unread-full-effects` dynamic · snapshot/nav lazy import() | `use-cm-participants-hub-sync.ts`, `cm-participant-unread-full-effects.ts`, `GlobalCommunityMessengerUnreadSound.tsx` | vitest hub-sync, `tsc` | Realtime 3종·overlay·call recovery·BN9~BN11 변경 금지 |
| 2026-06-14 | 하단 5탭·거래 목록 | **원인:** 440ms 슬라이드 후 **비메신저 handoff 1200ms 고정** + `HomeProductList` clientInstantBoot 시 `initialHasFullSeed` 미반영·썸네일 lazy — 탭 선택 후 본문·썸네일이 늦게 구현됨. **개선:** 슬라이드 440/250ms **유지**, pathname 도착 시 handoff 즉시 해제·RSC 미도착만 pending 유지; `initialBoot` 전체 행·상단 4장 `priorityThumb`. 메신저 bootstrap handoff 불변 | `AppRouteTransition.tsx`, `HomeProductList.tsx`, `PostCard.tsx` | `verify:trade-primary-tab-transition`, `tsc` | 비메신저 1200ms handoff 고정 복귀·슬라이드 duration 축소 금지 |
| 2026-06-14 | 하단 5탭·리스트 썸네일 | **근본:** (1) 메신저 탭 `guardNav` 가 commit·pending 패널 우회 (2) same-group router 전 2×rAF (3) push handoff remount 시 썸네일 pulse (4) 도메인별 lazy·priority 미설정. **개선:** 로그인 메신저 표준 commit·router 즉시·handoff bootstrap 즉시 해제·`thumbnail-loaded-url-memory`·philife/trade/messenger/stores above-fold priority. 슬라이드 440/250ms 불변 | `BottomNav.tsx`, `main-bottom-nav-route-commit.ts`, `AppRouteTransition.tsx`, `SamarketThumbnail.tsx`, `thumbnail-loaded-url-memory.ts`, `CommunityCard.tsx`, `feed-list-layouts.tsx`, `TradeProductThumb.tsx`, `StoreProfileThumb.tsx`, `StoresHomeFoodCard.tsx` | `verify:trade-primary-tab-transition`, `tsc` | 메신저 commit 우회·same-group 2×rAF router 지연 복귀 금지 |
| 2026-06-14 | 하단 5탭 전환 **실측 PASS 마감** | **측정:** `aaaa@manual.local`·`samarket:debug:navPerf=1`·Playwright `scripts/measure-bottom-nav-tab-verification.mjs` — 홈→거래→커뮤니티→배달→메신저→홈 × cold-1/warm-1/warm-2. **PASS:** (1) 비메신저 same-group handoff `@1200ms`/`@1600ms` 전 구간 false·`handoffSpans=0` (2) 메신저 `beginMenuNavigation` intent_sync **3/3**·`intentCommitMs=0~1` (3) warm 거래/커뮤니티/배달 `thumbPulseCount=0` (4) sync commit `intentCommitMs=0` — router.push 전 rAF/timer 대기 없음. **한계:** `clickToIntentMs` 는 `navPerfMarkBottomNavClickStart` 가 async navigate 직전에 찍혀 과대; `routeSettledMs` 는 dev RSC compile 영향( warm same-group ~2.5–5.5s ); 메신저는 pathname vs `?from=&section=` query mismatch 로 `routeSettledMs` null 가능. **범위 밖 TODO:** 배달 `(main)↔(stores)` cross-group full remount — 추후 stores same-group shell 통합 또는 stores push shell/keep-alive | `docs/trade-perf-hot-path-changelog.md`(본 절), `scripts/measure-bottom-nav-tab-verification.mjs` | `verify:trade-primary-tab-transition`, `npx tsc --noEmit`, Playwright 실측 | 비메신저 1200ms handoff 복귀·메신저 commit 우회·warm pulse 재표시 복귀 금지 |
| 2026-06-14 | **비로그인 401 네트워크 정리 PASS** | **개선:** `messenger-call-sound-config`·`fetch-me-notifications-deduped` — session gate · 401 retry 제거 · `unauthorizedUntil`/`authExitPaused` · logout pause. **실측 PASS:** guest `/stores`+탭시도+82s poll — sound-config **0** · list deduped **0** · 대상 API **401 0**; login cold — sound **200×1+** · list **200×3**. **INCOMPLETE:** headless 5탭 왕복 UI 자동화. **범위 밖 TODO:** badge unread 200 다회 · Console `me_profile_full`/`auth/session`/oauth — `docs/dibay-guest-401-network-cleanup.md` | `messenger-call-sound-config-client.ts`, `fetch-me-notifications-deduped.ts`, `resolve-client-authenticated-user-id-for-fetch.ts`, `invalidate-auth-exit-client-caches.ts`, `PhilifeHeaderNotificationInbox.tsx`, `MyNotificationsView.tsx`, `docs/dibay-guest-401-network-cleanup.md` | `npx tsc --noEmit`, vitest sound-config·notifications-deduped (10/10), Playwright guest/login 분리 context | session gate·401 backoff 제거 시 guest 401·poll 재발 — 본 문서 TODO 표 참고 |
| 2026-06-15 | 채팅 진입·메신저 타임라인 | **원인:** compose·기존 방 진입 시 `prefetchCommunityMessengerRoomSnapshot` fire-and-forget → `clientShellPlaceholder` 구간 버퍼링 스피너·첫 전송 시 낙관 메시지로 스피너만 사라지는 체감. **개선:** `router.replace`/`push` 전 `prefetchTradeMessengerRoomSnapshotWithCap` await(compose 6s·기존 1.4s cap)·RSC prefetch 병렬 | `trade-chat-room-route-prefetch.ts`, `TradeChatComposeClient.tsx`, `trade-chat-entry-navigation.ts` | `verify:trade-hot-path-contract`, `tsc` | 상세 동기 `createOrGetChatRoom`·compose 즉시 replace 계약 유지; snapshot prefetch void-only 복귀 금지 |

---

## 하단 5탭 전환 실측 (2026-06-14 PASS)

**시나리오:** 로그인+가입완료 · 하단 5탭 홈→거래→커뮤니티→배달→메신저→홈 · cold-1 / warm-1 / warm-2  
**도구:** `node scripts/measure-bottom-nav-tab-verification.mjs` · `[nav-perf]` · `data-main-shell-push-handoff` MutationObserver

### PASS 항목

| 항목 | 결과 | 로그 근거 |
|------|------|-----------|
| 비메신저 same-group 1200ms handoff 잔류 없음 | **PASS** | philife↔market cold/warm: `handoffAtRouteArrival`·`handoffAt1200`·`handoffAt1600` 전부 false; observer `handoffSpans=[]` |
| 메신저 `beginMenuNavigation` 표준 commit | **PASS** | cold/warm 메신저 3/3 `[nav-perf] intent_sync` · `intentToPath=/community-messenger?from=delivery&section=chats` · `intentCommitMs=0~1` |
| warm 거래/커뮤니티/배달 썸네일 pulse | **PASS** | warm-1/2: 거래 img 43·커뮤니티 2·배달 144 — `thumbPulseCount=0` · `thumbFramesWithPulse=0` |
| router.push 전 rAF/timer 대기 없음 | **PASS** | 전 탭 `intentCommitMs=0` (sync `beginMenuNavigation` 블록) |

### 측정 한계 (해석 시 분리)

| 한계 | 설명 |
|------|------|
| `clickToIntentMs` 과대 | `commitMainBottomNavRouteNavigateAsync` 에서 click 시각을 `beginMenuNavigation` **이후** 갱신 — 2~11s 값은 제품 지연이 아님 |
| `routeSettledMs` dev compile | warm same-group philife↔market ~2.5–5.5s — handoff와 무관한 RSC/dev compile |
| 메신저 `routeSettledMs` null | pathname `/community-messenger` vs intent `?from=&section=` 불일치로 nav-perf finalize 미기록 |

### TODO — 배달 탭 remount (이번 handoff/commit 범위 밖)

| | |
|---|---|
| **원인** | `(main)` ↔ `(stores)` route group **full remount** — dual-panel·handoff 경로 미사용 |
| **관측** | `/philife`→`/stores`·메신저 진입: `beginMenuNavigation` ✅ · `routeSettledMs` null · handoff/pushViewport 미관측 |
| **이번 수정과의 관계** | handoff 1200ms·메신저 commit·썸네일 memory 와 **독립** — 별도 트랙 |
| **추후 후보** | (1) stores 를 same-group shell 로 통합 (2) stores 전용 push shell / keep-alive 전략 설계 |

---

## 비로그인 401 네트워크 정리 (2026-06-14 PASS)

**전문:** [dibay-guest-401-network-cleanup.md](./dibay-guest-401-network-cleanup.md)

| 항목 | guest 실측 | login 실측 |
|------|------------|------------|
| `messenger-call-sound-config` | request **0** | **200×1+** |
| `me/notifications` list deduped | request **0** | **200×3** |
| 대상 API HTTP 401 | **0** | **0** |
| 하단 5탭 Playwright | **INCOMPLETE** (API 합산 PASS) | — |
| Console 401 잔류 | **TODO** (me_profile · auth/session · oauth) | — |

---

## P4 Active Call Session (2026-06-18)

| 날짜 | 영역 | 변경 | 파일(대표) | 검증 | 재발 방지 |
|------|------|------|------------|------|-----------|
| 2026-06-18 | 메신저 통화 | **ActiveCallSession SSOT** — Android FGS/PiP/screen-off keep-alive, iOS CallKit+AVAudioSession, JS presenter background·RECONNECTING, 서버 heartbeat·stale cleanup RPC | `lib/call/active-call-session-machine.ts`, `DibayActiveCallSessionManager.java`, `call-server-heartbeat.ts`, migrations `20260618140000_*`/`20260618150000_*` | `verify:active-call-lifecycle-contract`, unit tests | forbidden cleanup reason 가드·native `reportRemoteEnded` 브리지 유지 |
| 2026-06-19 | 메신저 1:1 방 | **Direct call SSOT** — `canStartDirectCallBetweenUsers`·friendships accepted 1순위·통화만 friends_only gate·스냅샷 `directCallGate`/friendship Realtime refresh·API deny codes | `direct-call-permission.ts`, `friendship-resolver.ts`, `service.ts`, `use-messenger-room-friendship-sync.ts` | vitest 30+, `tsc` | `friendshipPreload`·critical/defer tier gate 생략으로 스냅샷 중복 쿼리 금지 |

---

## P0 Safe Area SSOT (2026-06-20)

| 날짜 | 영역 | 변경 | 파일(대표) | 검증 | 재발 방지 |
|------|------|------|------------|------|-----------|
| 2026-06-20 | 전역 shell | **Safe area SSOT** — `--safe-* = max(env, --dibay-safe-*)`·Android `DibayWebSafeAreaBridge` inject·header/bottom nav/messenger hub shell·keyboard `--chat-bottom-inset` nav 분리 | `app/app-shell.css`, `DibayWebSafeAreaBridge.java`, `app-bottom-nav.css`, `conditional-app-shell-flags.ts`, `chat-viewport-shell-platform.ts` | `tsc`, `vitest chat-viewport-shell-platform`, adb probe | env-only·페이지별 padding 땜질·모델 분기 금지 |
| 2026-06-20 | 채팅 방 viewport | **역행** — keyboard/resize 계약 깨짐(키보드 open 시 상단 밀림). `chat-viewport-shell.css`·`chat-viewport-shell-platform.ts`·chat detail shell height 를 P0 이전 env()/vv gap 계약으로 원복 | `app/chat-viewport-shell.css`, `lib/ui/chat-viewport-shell-platform.ts`, `conditional-app-shell-flags.ts` | `vitest chat-viewport-shell-platform` | 채팅 keyboard resize 는 기존 계약 유지·safe area 는 shell/header/nav 만 |

---
