# Slice 4 — Profile / Trust STATUS

```text
SLICE 4 PROFILE/TRUST CODE LOCKED  (pending commit)
SLICE 4 DEPLOYED                   (pending)
SLICE 4 RUNTIME PASS               (pending)
SLICE 4 LOCK                       (pending)
```

## Scope

- Member `/mypage/trust` + home manner row parity via `buildMemberTrustSurface`
- Authority: `profiles.trust_score` (`?fresh=1`); session temperature must not clobber API
- Battery high tiers → `--dibay-green` / `#0B421A`
- Admin same-user trust field contrast (Slice 1 writer unchanged)

## Out of scope

Activity · Account IA · Admin Projection · Auth / Messenger / Call / Badge

## Deploy countermeasure

`bash scripts/deploy-prod-clean-worktree.sh`

## Password

Env / manual only — never in docs, commits, QA logs.
