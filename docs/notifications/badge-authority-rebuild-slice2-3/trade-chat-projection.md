# Trade Chat Projection (Slice 2-3)

```text
TradeRoomRowBadge = unreadMessageCount(tradeRoom)
TradeHubBadge     = unread Trade room count
```

Trade **status** events = A_member (Bell) — not Trade Hub chat digit.

Facts: `loadTradeStoreOrderUnreadRoomFactsFromParticipants` → trade bucket  
Builder: `tradeHub` / `tradeUnreadRooms`
