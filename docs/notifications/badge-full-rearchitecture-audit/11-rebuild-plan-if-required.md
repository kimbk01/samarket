# 11 — Rebuild Plan (if required · approval-gated · not started)

**Triggered by:** `DESIGN VALID — IMPLEMENTATION REBUILD REQUIRED`  
**Status:** PLAN ONLY

---

## Principles

1. Keep Phase 1 / restored product contract (A/B/C clear rules, App Icon formula).
2. Do **not** git reset to `1e2a560c1`.
3. Do **not** ship another isolated Bell filter as “product fix.”
4. Prove **ID sets** before declaring any axis LOCK.
5. One dual-source collapse per step; measure surfaces together.

---

## Recommended sequence

### Phase R0 — Freeze

- No more badge product patches until R1 evidence.
- Optional: selective revert candidates listed only (2-6 FCM) — **approve separately**.

### Phase R1 — A surface single identity (highest priority)

**Goal:** one unread A event ID set drives:

- Bell digit (decide: keys vs events — **product pick one**, document)
- Header list unread subset
- Full list unread subset  
- Mark-all / delete targets  
- Popup **A section** (if Bell popup remains A surface)

**Remove or quarantine:** messenger “중요 대화” from Bell A authority (move to chat chrome).

**Collapse:** legacy `notifications` from mark-all when events cover.

**Exit:** same-account dump: digit IDs ≡ list unread IDs ≡ mark-all IDs.

### Phase R2 — App Icon membership proof

- Dump A IDs + room IDs + missed IDs for FAIL accounts (Samsung 23 / iOS 9).
- Prove exclusions (owner rooms, C, ads).
- Exit: total = sum of set sizes; UI App Icon matches.

### Phase R3 — Native freshness

- Cap resume must not outrank Web Authority indefinitely.
- iOS Cap/lastApplied follow server after boot/resume/push/read.
- FCM always-send 0: keep or selective revert only with A/B proof.

### Phase R4 — Owner residual writers

- Route/stop `owner_intake` user_id as badge-adjacent; C remains Action RPC.
- Exit: no owner ops event can affect member A even if filter regresses.

### Phase R5 — Re-earn PASSes

- Axis harness + **product surface matrix** together.
- Never Cap-only → PRODUCT PASS.
- LOCK only after R1–R3 exit criteria.

---

## Explicit non-goals until approval

- Full implementation rollback of 2-2…2-6 without rewrite ready  
- Contract rewrite (DESIGN INVALID path)  
- Data mass zeroing  
- Working tree cleanup of unrelated dirty files
